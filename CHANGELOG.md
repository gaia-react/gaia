# Changelog

All notable changes to GAIA React are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

- **Major**: breaking changes to skill/command API, Node/React/React Router major bumps.
- **Minor**: new skills, commands, or wiki concept pages; opt-in features, removed or renamed `.claude/` paths.
- **Patch**: bugfixes, docs, and in-range dependency bumps.

## Adopter-action convention

A release change that requires the adopter to act, run a command or hand-migrate, is authored as a `### Removed` or `### Changed` entry carrying an explicit **Action required:** line and/or a literal command to run (for example, `pnpm remove <pkg>`). That exact phrasing is the deterministic anchor GAIA's two CHANGELOG consumers key on: `/update-gaia` cross-references it during a merge to surface a documented, opt-in cleanup suggestion (it never auto-removes a dependency or deletes a file, the adopter decides), and the maintainer-only `release-notes` skill keys on it to tell an agent-automated cleanup (reframe as a benefit or drop) from a human-must-act migration (keep, plainly framed, with a pointer to the steps). Keep the marker and command literal so both consumers match the anchor instead of free-parsing prose. The `react-router-dom` removal below is the worked example. `/update-gaia`'s confirm gate shows the full baseline-to-latest CHANGELOG range, so an adopter several versions behind sees every intervening entry.

## [Unreleased]

### Added

- the session-start janitor gains a ninth sweep that reaps off-pattern residue no other sweep owns: aged, off-allowlist entries at the top level of `.gaia/local/` and the direct children of its `audit/` and `cache/` dirs are removed once past a retention window (default 7 days, floor 2), while OS junk goes at any age. It is allowlist-gated so no live writer's output is mistaken for residue, and it never touches what an existing sweep already owns, any never-traverse working-state dir, or anything nested deeper. Two dedicated retention knobs also age out `audit/*.findings.json` and `cache/gh-artifact-pr.json`, and the `Local Working State` wiki page now enumerates all nine sweeps (#869)

- a `PreToolUse` guard (`.claude/hooks/block-invalid-yaml-write.sh`) that denies an Edit/Write/MultiEdit call that newly breaks a YAML region, a `.yml`/`.yaml` file or a Markdown `---` frontmatter block, closing the recurring footgun where a mid-sentence `: ` or a stray ` #` in an unquoted scalar reads as structural YAML instead of prose and is only caught later by a downstream parser or lint, sometimes on an already-saved artifact. The check is regression-only, so a file whose frontmatter was already broken before the edit never gets locked out (#867)
- a `PreToolUse` guard (`.claude/hooks/block-worktree-path-mismatch.sh`) that denies an Edit/Write/MultiEdit call whose target resolves to a different git worktree than the session's current one, closing a silent-wrong-write footgun where a stale pre-switch absolute path applied to the wrong checkout with no error signal. `isolation.md`'s `RESOLVED_ROOT` guidance now explicitly covers Edit/Write/Read alongside Bash and sub-agent dispatch (#842)

- New wiki concept page, Registering a Code Audit Team Member, documenting how to add a roster member (agent def, roster and builtin-fallback lockstep, machinery wiring, `finding_class` bucket, and recurrence-tally integration) and the required local-gate checklist a member add must run locally. (#817)
- Advisory prose-complexity lens for the Code Audit Team. A new maintainer-only, non-blocking member reviews changed instruction-prose under `.claude/skills/` and flags gratuitously complex prose (reducible length, deep nesting, high indirection, redundant instruction) as advisory comments plus a countable `prose/*` recurrence signal, holding GAIA's own instruction machinery to a legibility standard without ever blocking a merge.
- `/gaia-debt` gains a third handler class, `Handler: spec`, for tech debt whose fix has to begin with a design SPEC rather than a direct code change. A spec-class issue stays fully in the backlog (ordered, clustered, counted), but drains by printing a copy-pasteable `/gaia-spec` handoff and stopping instead of opening a fix PR; once handed off it parks under a durable `debt:spec-pending` label so it stops nagging until its SPEC and implementation land, or a maintainer removes the label. The handler value stays advisory like `prompt`/`plan`, so a drainer can still override it after reading the cited code, and the `Run /gaia-debt` count excludes parked issues (#815)
- the pre-merge Code Audit Team now reviews the maintainer CLI's build and config surface. `code-audit-maintainer-node` claimed only `.gaia/cli/src/**`, so an edit to the bundle build scripts and runtime dependencies (`.gaia/cli/package.json`), the resolved dependency tree (`.gaia/cli/pnpm-lock.yaml`), or the build config (`.gaia/cli/tsconfig*.json`) reached the required merge gate with no auditor ever reading it, exactly how a shell-portability defect in the bundle shebang once shipped to `main`. Those files now dispatch the member, kept in lockstep between `.gaia/audit-ci.yml` and the built-in fallback roster by `verify-audit-roster.sh`. Under that new coverage the orphaned `ulid` dependency left standing by the SPEC-038 CLI removal is drained, its own `package.json` edit now audited; nothing imported it, so the rebundled binaries are byte-identical. Maintainer-only and release-excluded: the maintainer-node member is scrubbed from the adopter bundle, so no adopter clone is affected (#813)
- the merge-time audit's recurring-finding tally now counts the GitHub Actions supply-chain findings the `code-audit-github-workflows` auditor previously could only report in prose (script injection, unsafe `pull_request_target`, unpinned actions, over-broad permissions). A closed `workflow/` finding-class bucket gives them a stable class so they feed the `/gaia-harden` recurrence loop, and a PR-time CI gate now verifies the committed CLI binary is reproducible from its source, so the audit and harden tooling always runs current code (#811)
- the pre-merge Code Audit Team gate now audits **locally by default**: a repo with no override runs the audit on the contributor's own machine instead of in CI, trading CI minutes for faster local feedback, and the default stays tunable per-team or per-author in `.gaia/audit-ci.yml`. A pull request from a fork still always resolves to CI, which runs under a scoped token rather than a contributor's full local credentials. A new `code-audit-github-workflows` roster member reviews GitHub Actions workflow and composite-action YAML for injection, permission, and secret-handling defects and reaches every existing adopter automatically on their next `/update-gaia`, and a new deterministic check verifies the roster's file-ownership rules are unambiguous before any of it ships (#810)
- a maintainer-only drift check for the branch ruleset's required status checks (`.gaia/scripts/verify-required-checks.sh`, plus an advisory-only CI job on pull requests that touch workflows). The set of checks that actually blocks a merge lives GitHub-side and can drift silently from the set the team believes is required, as #800 showed when a red `distribution-audit-pr.yml` run turned out not to be in the required set at all. The script declares the deliberately-required contexts in-repo and diffs them against the live ruleset, then separately flags other check-producing jobs that are not required so an intentional omission is a visible decision rather than an accident. It never mutates the live ruleset: promoting a check to required stays a deliberate, maintainer-run call, matching the ask-first cutover precedent for registering `GAIA-Audit`. Maintainer-only and release-excluded, since the declared contexts and the ruleset they describe are specific to the GAIA team's clone (#808)
- a per-PR distribution gate that fails a pull request introducing a newly-shipping file the committed `.gaia/manifest.json` does not answer, so each feature makes its own ship-or-withhold decision at its own PR instead of letting the manifest accumulate an unanswered backlog left for a later, unrelated `/distribution-audit` run. A maintainer-only `distribution-audit-pr.yml` CI job runs `gaia-maintainer release manifest --check` and fails only on the intersection of its unanswered set with the files the PR touches, so a pre-existing backlog never blocks an unrelated PR and each PR answers exactly the shipping surface it introduced. The PR-time complement to the release path, which regenerates the manifest with an explicit escape hatch by design. Maintainer-only and release-excluded: the maintainer binary and the manifest exist only on the GAIA team's clone, so the gate has no adopter surface (#785)
- a team-level git isolation policy, so GAIA stops asking branch-vs-worktree on every plan orchestration and every `/gaia-debt` run. A project owner picks the team's strategy once, when `/gaia-init` scaffolds a new project or when `/setup-gaia` runs on an existing one, and the choice is stored as an `isolation_policy` key in the committed `.gaia/automation.json`, so it travels with the repo instead of living on one machine. Choose to always use worktrees, to prefer them, or to prefer branches; the question explains what worktrees buy and what they cost before you answer. Answering is entirely optional and a team that never answers sees exactly today's behavior: the key stays absent, the prompt keeps asking, and the question stays available to answer later. The policy governs `/gaia-plan` and `/gaia-debt` only; `/gaia-audit`, `/gaia-harden`, and `/gaia-wiki` continue to work in the main checkout (#760)
- the six maintenance commands (`/gaia-audit`, `/gaia-debt`, `/gaia-fitness`, `/gaia-forensics`, `/gaia-harden`, `/gaia-wiki`) now record what they cost. Each run appends exactly one `kind: "command"` row to the local cost ledger and prints its spend as the last line of its report, closing the gap that let six of GAIA's nine token-spending commands burn a full session and account for none of it. Cost rows also carry the pull request or issue the run produced, so spend ties back to the artifact it bought; a command binds only the artifact its own run opened, never one it looked up or inherited, so an unattributed run records no artifact rather than a fabricated one. Producer-side only and fully back-compatible: ledger `schema_version` stays `1`, every new field is optional, and existing consumers are unchanged. A new `PostToolUse` hook (`.claude/hooks/capture-gh-artifact.sh`) watches `gh pr create` so plan execution, the one surface with no agent in the loop, can bind its pull request with no network call on the commit path; it exits 0 unconditionally and degrades silently. `wiki/concepts/Cost Data Contract.md` pins the shapes a dashboard can consume without reading the source (#758)
- `/distribution-audit`, a maintainer-only command that makes the ship-or-withhold decision for a newly-shipping file cheap and explicit at the moment the file appears, instead of leaving it to be noticed on release day or not at all. It names each file that would newly reach adopters and asks the maintainer to ship or withhold it, one file at a time, and it drives the release CLI, which now refuses by default, in every output mode, to produce a manifest at all while any shipping file lacks an explicit answer. Withholding writes the entry into the distribution boundary through the CLI, which is what makes the literal-path rule a mechanism rather than a promise. The release path keeps regenerating unconditionally via an explicit escape-hatch token a reader can see in its source. Maintainer-only tooling, so no adopter clone is affected (#686)
- a deterministic `shellcheck` gate over GAIA's own framework bash (`.gaia/tests/shell-lint.sh`, run by a maintainer-only `shell-lint.yml` CI job on every PR that touches a shell script). `shellcheck` was already the `code-audit-maintainer-shell` agent's authoritative oracle, but that agent is model-dispatched and advisory-only, and no other surface ran the tool: not CI, not the Quality Gate, not the pre-commit hook, which fires only on `app/`, `test/`, and `.storybook/` and so skipped shell changes entirely. A hand-maintained lint is a ratchet with no pawl. The gate lints every tracked `*.sh` at severity `warning` and fails the PR on any finding, while the agent keeps the lenses `shellcheck` cannot model (hook fail-open, stdin-JSON shape, `jq -n` injection safety). Turning it on surfaced a malformed `shellcheck` directive that had been silently aborting the linter's parse of an entire file, plus dead code in two shipped scripts. Maintainer-only and release-excluded: adopters run GAIA's bash but never author it, so the linter guarding it has no adopter surface (#689)
- `/gaia-spec` is now terminal in enforcement, not just in prose: a session that authors a SPEC can no longer chain into `/gaia-plan`. Planning always starts in a fresh session, because a spec session ends with an enormous context and the planner needs a clean one, but a skill-invoked `/gaia-spec` carries an outer goal ("spec and build X") that outlives the skill body, and the printed handoff was the only thing asking it to stop. A new `PreToolUse` guard (`.claude/hooks/block-spec-plan-chain.sh`) stamps the session when a SPEC id is allocated, then denies both routes into planning: the `Skill` call, and the `Read` of the plan reference an agent would otherwise follow inline. It is scoped to the session that ran `/gaia-spec` and released by `/clear`, so a fresh session still reaches `/gaia-plan` by any route. The block is strict: a `/gaia-plan` you type yourself in that same session is denied too, and the deny names the one-keystroke path (`/clear`, then paste the handoff) (#684)
- a `file-tech-debt` skill that is the single source of truth for filing an out-of-scope code-review finding as a deduplicated, severity-labeled `tech-debt` GitHub issue (dedup key, `--body-file` filing, idempotent labels, self-contained body schema, and the debt-count sentinel touch). The `code-review-audit` agent and `/gaia-audit` now reference the skill's recipe by path rather than each carrying an inline copy, and it auto-triggers in conversation on natural-language asks like "file this finding as debt". The wrapped `gaia-debt-key` format and label spellings stay a contract shared with the deterministic debt-count consumers (#628)
- the Code Audit Team: GAIA's pre-merge code review becomes a config-driven roster in `.gaia/audit-ci.yml` that maps file-globs to specialized auditors, all feeding one shared `GAIA-Audit` merge gate that fails closed until every dispatched member clears. The local gate is now a real AND-aggregator, so one cleared member can no longer satisfy the gate while a co-dispatched member withholds its marker over a Critical finding, and the member-aware status POST holds the github.com UI merge button shut on the same condition; `GAIA-Audit` is confirmed as the required check under both classic branch protection and repository rulesets. Two maintainer-only members now review the framework source the frontend auditor never could, `code-audit-maintainer-shell` (framework bash, with a `shellcheck` oracle and a hook-contract lens) and `code-audit-maintainer-node` (framework `.gaia/cli/src` TypeScript), both advisory-only and scrubbed from the adopter bundle. The frontend auditor is renamed `code-review-audit` → `code-audit-frontend`; the adopter-clone rename and roster migration are deferred to the v1→v2 update session (#626)
- a maintainer-only Comprehensive Audit phase for `/health-audit`: after the integrity loop terminates and before the final verdict, a deterministic gauge picks depth (skip / scoped / full) from the diff since the last release tag, then one specialist auditor per dimension (feature coherence, distribution integrity, `.gaia` tidiness, and the audit's own machinery) fans out over its slice, every material finding is adversarially verified by a refuter, and a delegated writer composes a prioritized report the maintainer dispositions by hand. Report-only (no auto-heal, files nothing) and entirely release-excluded, so nothing ships to adopters (#617)
- audit-phase spend is now attributable in the local cost ledger. The adversarial spec/plan audit (the `/gaia-spec` and `/gaia-plan` fan-outs) records as a nested `audit.adversarial` subset annotation on its phase cost record, carrying its own token buckets, dollars, elapsed time, and lens set as a strict drill-down that never changes the phase total; and each `code-review-audit` run records exactly once as a standalone `kind:"review"` cost row, associated to the SPEC or plan on the branch (or left unattributed when ad-hoc) through a Stop and `gh pr merge` trigger and tagged with its `source`. Producer-side only and fully back-compatible: `schema_version` stays `1`, every field is optional, and the existing cost-ledger consumers are unchanged. Dashboard rendering of the new drill-down is a fenced follow-on (#613)
- a two-tier OS-sandbox enablement preference. `/gaia-init` records a committed, GAIA-namespaced `sandbox_recommended` intent in `.gaia/automation.json` (never Claude Code's real `sandbox.enabled`), and `/setup-gaia` resolves it per machine: it detects sandbox capability (macOS ready, Linux/WSL2 needs `bubblewrap`+`socat`, native Windows/WSL1 unsupported), prompts once with the owner's recommendation as the default, and writes the real enable only to the gitignored `.claude/settings.local.json` with a minimal starter config and a per-machine resolution marker. `gaia ping --event setup` gains a `--sandbox on|off` field, and a new `OS Sandbox` wiki concept page plus a setup message state honestly how far the `.env` deny-merge actually protects secrets. Composes with the SPEC-028 read-side guard without modifying it (#607)
- linked git worktrees now share the main checkout's gitignored root `.env` / `.env.*` files (excluding the committed `.env.example`) through the same symlink mechanism that already shares `.gaia/local/` state, so a worktree's `pnpm dev` server and Playwright read the same local secrets with no manual copy. The symlink stays gitignored in the worktree and the read-side `.env` guard still denies it by basename, so the security posture is unchanged; the manual `gaia setup link-worktree` command gains the same behavior (#608)
- a static lint gate (`.gaia/scripts/lint-hook-array-guard.sh`) that flags unguarded bare `${arr[@]}` / `${arr[*]}` expansions in `set -u` hook bodies, the bash 3.2.57 empty-array abort class the bash-5 test suites cannot see, so a hook that would silently abort on stock macOS `/bin/bash` is caught before merge on every bash version. A sibling bats suite runs it over `.claude/hooks/**` in the existing audit CI job and self-tests the detector against a known-bad fixture; the one provably-safe expansion the scan flags in `block-rm-rf.sh` is offset-guarded so the gate stays zero-exception (#604)
- a read-side `.env` secret guard (`.claude/hooks/block-env-read.sh`) that closes the real gaps the literal `Read(.env)` deny leaves open. It denies the Read tool and residual Bash reads (sourcing, redirection, readers outside the recognized cat/head/tail/sed set) against the `.env.<env>` variant family (`.env.local`, `.env.production`, nested) the exact-`.env` rule never matched, plus recognized readers against those variants and bare `env`/`printenv` environment dumps, while keeping `.env.example` reachable and never echoing the matched command into the deny reason. The two canonical wiki homes and the hook header now state the honest posture: what `Read(.env)` already enforces against the Read tool and recognized Bash readers at any depth, the residual gaps closed, that the guard is heuristic defense-in-depth and not a sandbox, and that the OS-level `sandbox.filesystem` deny-read (merged with the Read/Edit deny rules) is the airtight opt-in that reaches arbitrary subprocesses (SPEC-028) (#601)
- git-arbitrated phase-level resume for the plan orchestrator: a cold-restarted plan run detects a prior attempt from its `RUNNING` sentinel, reconnects to that run's branch by isolation mode, and a deterministic helper (`.gaia/scripts/plan-resume-point.sh`) proves which phases are genuinely committed (each phase's recorded commit an ancestor of the reconnected HEAD) so the run resumes at the first incomplete phase behind a Resume / Restart / Abandon gate instead of re-dispatching sub-agents against work already in the tree. Git is the arbiter and any ambiguity re-runs the phase; the `RUNNING` sentinel gains a `mode:` line and each `SUMMARY.md` phase block leads with a machine-readable `Commit:` anchor, with the plan reference the single source of truth for that block format (#600)
- the `.gaia/cli` maintainer package is now linted to GAIA standards: a dedicated `.gaia/cli/eslint.config.mjs`, a `pnpm lint:cli` script, and a `cli-tests.yml` CI step that enforces it on every push, so the CLI holds the same quality bar as the app (#592)
- adoption pings now cover `setup` and `update` in addition to `init`, unified under a shared `gaia ping` subcommand, and carry a stable per-install `projectId` (#585)
- a per-agent reliability guard on the shared adversarial-audit fan-out that `/gaia-spec`, `/gaia-plan`, and the pre-merge `code-review-audit` all run: a shared deterministic no-op detector (`.gaia/scripts/audit-noop-detect.sh`) classifies each dispatched auditor's on-disk output without loading a finding body into the orchestrator's context; a silently no-op'd auditor gets exactly one hardened first-tool-call retry and then a guaranteed inline fallback that records degraded coverage instead of counting the lens as "found nothing"; and every audit dispatch prompt now leads with a positive first-tool-call guard line (#579)
- spec-less one-off plans now receive a monotonic `PLAN-NNN` id allocated from a local, per-machine `.gaia/local/plans/ledger.json`, replacing the description-derived slug folder. This gives an ephemeral plan the same stable ordering and allocation record a SPEC gets, so "which was plan #3" is answerable and the folder name is a clean telemetry/feature key. (#565)
- `/gaia-serena-sync`, a consent-gated command plus a passive statusline nudge that keeps Serena's indexed languages in step with a polyglot project. Serena picks one language at first startup and never re-detects, so a project that later grows Go or Python keeps getting single-language symbol intelligence with no signal. When a git-tracked high-signal manifest names a language absent from the `languages:` list in `.serena/project.yml`, the statusline surfaces the drift within a refresh cycle and, on an explicit yes, the command additively appends the missing language(s) to that list in place and byte-identically, then prompts a Serena restart. It never mutates the file without consent, refuses any config form it cannot edit safely, and stays inert for adopters who do not run Serena (#559)
- a per-phase dollar-cost figure in the cost sidecar the token tally writes: planning, execution, and spec-authoring costs each carry an estimated-USD figure, priced from that phase's own per-model token usage using a committed rate table and effective-dated pricing shared with the merge roll-up via one lib (`.gaia/scripts/token-pricing-lib.sh`) that both the tally and the roll-up source. Pricing never guesses a rate and never blocks: input it cannot price degrades to an `unavailable` marker, undercounted input to a marked `lower bound`, and the tally always exits 0 (#558)
- a dollar-cost estimate on the full-cycle token roll-up: it appends an `Est. cost (USD)` block that prices each action's tokens per model and per billing bucket from a committed, adopter-tunable rate table (`.gaia/scripts/token-rates.json`), applying cache read/write multipliers and effective-dated introductory pricing. It never fabricates a figure: input it cannot price degrades to an `unavailable` marker and undercounted input to a marked `lower bound`, and the roll-up always exits 0 (#547)
- a per-action ground-truth token and wall-clock cost readout for `/gaia-spec`, `/gaia-plan`, and KICKOFF plan execution. At each action's completion point a new `.gaia/scripts/token-tally.sh` helper sums the four billing buckets (fresh input, cache write, cache read, output) plus a total across the session's main transcript and every sub-agent sidecar, deduped to what the API actually billed, reports the wall-clock elapsed over billed model turns with the start and end shown in the machine's local timezone, writes a `cost.json` sidecar beside the spec or plan (a plan's sidecar keeps planning and execution costs distinct and never overwrites or sums them), and appends a durable machine-local ledger record (`.gaia/local/telemetry/cost.jsonl`) resolved to the main checkout so it survives deletion of the plan folder and a linked worktree. The tally never blocks or fails its action and degrades to a partial figure with a marker on unreadable input rather than fabricating a number (#536, #546)
- a Serena code-search enforcement guard (`.claude/hooks/serena-code-search-guard.sh`): a PreToolUse hook that blocks a bare-identifier `Grep` over `app/**` or `test/**` TS/TSX and routes it to Serena's LSP-backed symbol tools, closing the gap where the edit-scoped routing rule was absent from context during exploration. Re-running the identical grep passes for a genuine string or comment search, and the guard no-ops unless Serena is a registered MCP server with a `tsconfig.json` present, so adopters without Serena are unaffected (#511)
- `gaia.updateDepsHold`, a committed per-package version hold for `/update-deps`: a `{ package: ceiling }` map in `package.json` that caps a package to a version-prefix line (`"8.0"` holds the highest `8.0.x`, `"8.0.16"` freezes exactly). Unlike the CI-ignored local snooze ledger, the hold is committed, so it applies in interactive and CI runs until the maintainer lifts it, and fails closed when the ceiling matches no published version. The `/update-deps` preview surfaces active holds (#500)
- output-verification hardening for three GAIA self-audit workflows, so a single-pass judgment can no longer drive an edit or a clean verdict unchecked. `/gaia-audit` gains an optional classification-verification round before its Apply / Discuss / Decline gate (biased toward keeping any flagged memory delete it cannot confirm, since those deletes are irreversible); `/health-audit` (maintainer-only) gains a false-clean challenger that can revoke a clean exit; and `/gaia-fitness`'s final verify cycle now re-runs all seven categories, so a regression in a zero-finding lane is caught before it reports A+. All rounds are recommended-but-optional and never block (#472)
- a SessionStart janitor (`.claude/hooks/local-janitor.sh`) that garbage-collects dead working-state residue under the gitignored `.gaia/local/` on every startup/resume. It removes only residue whose death is provable, orphaned merge-gate markers and audit re-run ledgers, completed-but-unswept plan dirs, stray empty dirs, local `wiki-sync/*` landing branches whose upstream is gone, and orphaned merged worktrees left by a crashed session, so it is a backstop, not an owner; any unprovable death is skipped and the hook always exits 0 so it can never block a session from starting. It also owns retention for stale cache artifacts nothing else reaps, age-rotating them out of `.gaia/local/cache/` after 14 days. Adds the `Local Working State` wiki concept page (#468, #506, #673, #727)
- a forced-disposition gate on out-of-scope `code-review-audit` findings, plus the `/gaia-debt` drain skill. Debt the audit opens within its review radius but the PR did not change no longer slips through, non-security findings file as deduped, severity-labeled `tech-debt` GitHub issues, while security-class findings are fail-safe classified and diverted off every public or enterprise-readable channel (a private issue only on a confirmed private repo). The gate is the audit's fourth marker precondition, and `/gaia-debt` drains one `tech-debt` issue per invocation (deterministic severity-then-age ordering, a fresh branch through the normal audit gate), surfaced by a `Run /gaia-debt (N issues)` statusline indicator. Adds the `Audit Disposition and Debt Drain` wiki concept page (#465, #475, #495, #496)
- a maintainer-only CHANGELOG gate in the PR merge workflow: before any `gh pr merge`, evaluate whether the change warrants a `## [Unreleased]` entry and, if so, land it on the PR branch before merging, re-checked on every merge (including PRs resumed across sessions) so an entry promised at authoring time can't be lost. Lives in the `PR Merge Workflow` wiki page and the always-loaded `pr-merge` rule, both maintainer-only (#446)
- `/gaia-react-perf`, a measure-only React render-performance diagnostic: it drives a micro-interaction, captures per-render attribution through a committed bippy Playwright harness, and presents a ranked `memoDefeated` diagnosis with a react-doctor cross-reference and a structural-first fix. v1 is measure-only, the human applies the fix in conversation. Adds the React Perf Diagnostic wiki concept page and registers the skill so `/update-gaia` ships it (#442, #443)
- a React 19 idiom gate in the `react-code` skill (Gate 4): ref-as-prop over `forwardRef`, boolean-guarded `&&` over the numeric-0 leak (coercion mandatory, with react-doctor's `rendering-conditional-render` rule as the pre-merge backstop), `use()` and the `<Context>` shorthand over `useContext`/`Context.Provider`, never `null` in render, and staying in React Router's lane for forms. `references/hook-patterns.md` gains `useEffectEvent` and ref-callback cleanup, and the `typescript` skill prefers `!!` over `Boolean()` for inline coercion (#441)
- a `remix-utils` awareness map (`wiki/dependencies/remix-utils.md`): a curated, risk-sorted decision map (hydration-safe client render, SSE, CSRF, honeypot, safe-redirect, debounced fetcher) reachable from the surfaces Claude loads before hand-rolling a primitive. `MetaHydrated` now derives its hydration signal from remix-utils' `useHydrated()`, so `remix-utils` becomes a genuine import and drops out of knip's `ignoreDependencies` (SPEC-007) (#427)
- an adversarial multi-agent SPEC-audit in `/gaia-spec` that runs automatically on every spec before the final gate at a gauged Standard or Deep tier, with no prompt and never blocking save (the only non-run path is a fan-out-unavailable fallback): low-overlap lenses verify the draft's checkable claims against the repo and `node_modules` rather than on faith, a refutation pass keeps severity honest, and each surviving finding becomes either a plan-time directive (recorded in a sibling `AUDIT.md`) or a contract fix folded into the draft pre-save. It runs as a parallel `general-purpose` Agent fan-out so it works headless, and keeps the main thread lean by holding finding bodies in a per-spec on-disk cache while a delegated applier folds the fixes (SPEC-011) (#423, #483, #874)
- a lightweight adversarial decomposition audit in `/gaia-plan` that runs automatically after the plan is generated on any non-trivial plan and is skipped on a trivial single-phase one, decided by a gauge with no prompt and never blocking the handoff: three parallel lenses verify the decomposition itself, the one artifact neither the upstream SPEC-audit nor the downstream pre-merge `code-review-audit` can see, checking task-graph soundness, that every file, export, type, and signature named in a task contract resolves against the real repo and `node_modules`, and SPEC coverage. It is deliberately narrower than the SPEC-audit (no refutation pass, since the findings are checkable); localized findings fold into the task docs, a structural finding re-spawns the planner (#424, #874)
- `react-code` skill leads with a platform-first ladder (existing GAIA code → web platform like `Intl`/`URL`/`crypto.randomUUID` → already-installed dep → new dep → custom code) to walk before adding a dependency or hand-rolling a primitive
- `/gaia-harden` weighs an efficacy lens (Axis 3) before recommending a form: a recurring finding proves the problem, not the fix, so when the recommended form is prose and no cheap before/after evidence shows it would change behavior, that surfaces as a defer/decline signal for the human, never an auto-decline
- point Zod schema work at Zod's official LLM docs and treat them as authoritative over training memory, so valid Zod 4 forms are not rejected from stale v3 recollection
- a TDD testing-strategy foundation (SPEC-006): an AST determinism classifier scopes the hard RED commit gate to deterministic tests, so affirmatively-emergent test files are no longer forced to show a natural RED; a second `gh pr merge` gate proves the worthiness extractor ran over changed emergent tests; an advisory two-axis (honesty and worthiness) audit surfaces keep/fix/delete proposals with every delete human-gated; and `new-component` scaffolds a non-degenerate a11y test that can actually fail. Adds the Determinism Classifier, Worthiness Audit, and Worthiness Presence Gate wiki pages (#408)
- per-author `code-review-audit` mode (local vs CI): the pre-merge audit gate keys on a `GAIA-Audit` commit status resolved per PR author, so a developer in local mode runs the audit on their machine while CI stands down, the branch stays protected, and the gate fails closed to CI whenever a local run cannot confirm the required check. `gaia-init` and `setup-gaia` gain audit-mode prompts, and `/update-gaia` field-merges `.gaia/audit-ci.yml` so a committed `audit_authors` entry never forces a whole-file conflict (#407)
- restore the wiki hot cache after a context compaction through two GAIA-owned hooks (a `PostCompact` sentinel writer plus a `UserPromptSubmit` re-injector), so the "where we left off" cache survives compaction without depending on a claude-obsidian hook that some Claude Code builds reject (#386)
- a manifest-write guard that keeps GAIA's own workflows from cataloging an adopter project's files into `.gaia/manifest.json`. The manifest is a release-generated catalog of the files GAIA ships; a path absent from it is adopter-owned. A new PreToolUse hook (`.claude/hooks/block-manifest-write.sh`) denies manifest writes through the edit tools and common Bash vectors (best-effort defense-in-depth, not airtight), and a new `.gaia/**`-scoped rule (`.claude/rules/gaia-folder.md`) declaring the whole `.gaia/` tree GAIA's own machinery rather than part of the app built on it (the manifest prohibition folds into it), plus the `/gaia-plan` reference's task-authoring and audit-lens sections carry an enforced prohibition so a planner or auditor is steered away from inventing a "register every net-new file" obligation. A polluted manifest misleads the tools that read it (the fitness per-file drift check, forensics capture), wastes a plan phase per feature, and is simply wrong data; `/update-gaia` never overwrites an adopter file from a manifest entry

### Changed

- Bare `/speckit-clarify` now redirects to `/gaia-spec` instead of running spec-kit core's clarify loop, which wrote an off-shape artifact and enforced a 5-question cap GAIA no longer uses. Adopters who want the core clarify primitive directly lose that path (#840)
- The pre-merge Code Audit Team now handles an out-of-scope finding whose file the same PR is already editing more usefully. A non-security, prompt-shaped finding in a changed TypeScript file inside the self-heal repair boundary is repaired in-branch through the audit's existing self-heal path instead of being filed as a `tech-debt` issue the author has to triage later, and every finding still filed carries the best-effort root-cause class the audit assigns to in-ledger findings (the `holistic/unclassified` fallback is reserved for a genuine no-map) so the `/gaia-debt` clustering rule can start batching the backlog. The out-of-scope dedup now matches an already-open issue on file path and line alone, ignoring the class, so re-running the audit after a finding is reclassified never double-files. Note that a reclassified out-of-scope finding of Important severity becomes countable in the `/gaia-harden` recurrence tally. (#814)
- Each Code Audit Team member's pre-merge clearance is now keyed to a content digest over exactly the files that member owns plus the shared gate machinery, not to a digest of the whole repository tree, so an approval survives any change that could not have affected what that member reviewed. An out-of-glob edit (a CHANGELOG line, a wiki page, a merge commit from main) rotates no member's key, so every existing clearance keeps validating and nothing is re-dispatched; a change to a file a member owns rotates only that member's key, re-auditing exactly the member whose content moved; and a change to the gate machinery, anything that can alter what a member reviews, who reviews it, or whether a clearance is believed, rotates every member's key by construction. Content addressing also makes the members order-independent: they can run in parallel, and the `GAIA-Audit` trailer stamp, an empty commit that leaves every blob byte-identical, no longer voids the clearances that preceded it. The narrower key does not loosen the gate, which stays fail-closed: a refusal for the current digest is checked before the earned clearance and is absolute, a valid frontend clearance whose out-of-scope disposition sidecar is missing denies the merge, and every clearance check returns false when `jq` is absent rather than degrading to a bare-existence match. (#800)
- A Code Audit Team member that already cleared a tree is no longer re-spawned by a later commit that changes nothing that member owns, so the fix-the-findings loop and a ride-along wiki or CHANGELOG commit stop paying for a full re-audit of otherwise-unchanged code. Clearances are now written by one shared script with a versioned body that the merge gate reads instead of trusting a bare filename, so any clearance marker already sitting in a developer's pool from before this release is re-earned once. The clearance pool is retained for 72 hours past reachability, tunable via `GAIA_AUDIT_MARKER_RETENTION_HOURS`.
- The interactive Socratic ceiling in `/gaia-spec` rises from 5 to 10 substantive questions, and GAIA owns the number outright rather than inheriting spec-kit's; auto mode holds at 5. The clarify loop is decoupled from `/speckit-clarify` entirely, so no spec-kit-authored question budget reaches the driving agent, and it gains a real coverage scan (Clear / Partial / Missing over the topic bank) whose "no topic is Partial or Missing" is the stop condition, so the higher ceiling bounds the loop rather than baiting it: a simple feature still settles in the two to four questions it does today, and raising the ceiling does not raise the floor. The coaching signal is made safe for the new regime, since the intent-clarity-gap pattern no longer pools auto-mode runs with human-driven ones and now reads a session's question count against the ceiling that was in force, so raising the ceiling cannot by itself strengthen a pattern whose own advice is to lengthen the question phase; telemetry already on disk is read at the ceiling it ran under, so no adaptation changes state as a side effect. A bare `/speckit-clarify` invocation is unchanged, and the spec self-review still runs. (#694)
- `.claude/rules/code-search.md` is now the single source for the Serena-vs-Grep symbol-tool routing, including the `rename_symbol` guidance that previously lived only in the always-loaded override; `serena-cc-override.md` shrinks to a short session-start pointer that keeps only its Opus-default nudge. This removes the duplicated routing guidance that had drifted between the two rule files. (#671)
- `/gaia-fitness` and `/gaia-harden` now finish the job instead of leaving changes uncommitted for you to publish by hand. After healing and showing the report card, `/gaia-fitness` gates once on a single publish confirmation, then commits, opens a PR, and merges it on a main-branch run (commit-and-push on any other branch), the same out-of-scope-bypass flow `/gaia-audit` uses. `/gaia-harden` opens the branch and PR for you once you have approved at least one change, and offers to merge it only when you approved every candidate in the run; a selective run (any decline or defer) leaves the PR open for review. (#670)
- `/gaia-debt` now claims each drain unit with a gaia-owned `debt:in-progress` GitHub label the moment it picks a ticket, so two concurrent drains on the same checkout stop offering each other the same work: a claimed issue drops out of the open-debt count and a peer session's `Run /gaia-debt (N issues)` nudge, and is excluded from that session's own candidate and batch offers. A drain reconciles stale claims left by an ungraceful session death before it reads the backlog (a claim stays live while a `debt/…` branch names the issue, an open PR closes it, or it was touched within a ~30-minute grace) and releases the label on a controlled stop or a security divert; `list` annotates a claimed issue `[in progress]` and `why` reports its claim status. The batch branch name changes to `debt/<members-joined-by-dash>-batch`, which names every member so the reconcile's branch check protects the whole batch. (#638)
- `/gaia-debt` now drains one drain unit per invocation rather than strictly one issue: it deterministically detects related `tech-debt` issues (same file, or same finding class within one directory) and recommends fixing a related batch together as one branch, one PR, and one audit marker that closes every issue in the unit on merge, while one-at-a-time stays an explicit, always-available choice and security-class issues are kept out of public batches. Each drain also prompts feature-branch-in-place vs git worktree the same way the plan orchestrator does, once per drain. (#635)
- `/gaia-spec`, `/gaia-plan`, and the plan-execution closeout now report cost as one pinned, terse line (`Cost: ~<total> tokens, $<dollars>, <elapsed>`, with a per-stage `(spec $… + plan $…)` breakdown when multiple stages exist) instead of a free-form agent summary that drifted run to run and, at spec/plan time, omitted the dollar figure entirely. Instruction-only: the token buckets, pricing, and ledger are unchanged, and unpriced input still degrades to `cost unavailable` rather than a fabricated number. (#634)
- unifies the spec and plan ledger status vocabularies into one set (specs `draft | ready | merged | abandoned`; plans `ready | merged | abandoned`), retiring `specified`, `allocated`, `completed`, `archived`, and `in-progress` as ledger statuses. **Action required:** none: a one-time migration rewrites every existing `specs/ledger.json` and `plans/ledger.json` row onto the unified vocabulary automatically, the next time the SessionStart janitor runs, and is idempotent, so the only visible effect is that local ledger status values change. (#609)
- a merged SPEC folder's `SPEC.md`, `AUDIT.md`, and `cost.json` are kept at merge, gated on the folder's cost being fully represented in the durable `.gaia/local/telemetry/cost.jsonl` ledger, and age-reaped by the SessionStart janitor only once past a retention window (`GAIA_SPEC_RETENTION_DAYS`, default 30); spec-less `PLAN-NNN` folders and colocated plan subfolders still delete at merge. The fail-closed pre-delete cost gate reads the `cost.json` sidecar (an unparseable sidecar still blocks a delete, the SPEC-026 invariant intact). The shared ledger write behind this is concurrency-safe under parallel worktrees (locked, with timeout-safe degradation so no row is ever lost), and a separate, human-gated one-time migration is available to clear the pre-existing `archived/` backlog under the same cost-representation guarantee. The plan orchestrator reconciles its id-ledger the moment its PR merges (new `plan-reconcile.sh` for the plan arm, `spec-reconcile.sh` for the spec arm). (SPEC-029) (#606)
- the bats-assertion-hygiene rule now also warns against a `!`-negated absence assertion used as a non-final line in a bats `@test`: POSIX `set -e` exempts a `!`-inverted command, so on every bash version the assertion silently greens even when its bad case is true. It directs the positive-match-plus-`return 1` form instead (#605)
- the audit dispatch narration in `/gaia-spec`, `/gaia-audit`, and the maintainer-only `/health-audit` challenger now names each lens in full with its id code in parentheses (e.g. `factual grounding (FG)`) instead of bare acronyms, so the workflow output says which lenses it is running in terms an adopter can read. (#599)
- `/gaia-audit` now runs the full flow `/update-deps` and `/gaia-debt` use: the up-front Apply / Discuss / Decline gate stays the single decision, and on Apply the run files any out-of-scope problem it can't fix (wiki-internal redundancy, a page-vs-page conflict, a doc that needs a rewrite) as a deduped, severity-labeled `tech-debt` issue, then commits, opens a PR, and merges it on a main-branch run (a non-main or CI run commits and pushes only; an empty diff no-ops). Findings that used to vanish into an unread Summary line now get a durable home. (#597)
- the `Run /gaia-audit` statusline nudge now names the concrete per-machine drift instead of the generic `machine drift`: it shows `N new memories` when machine-local memory has grown past the audit threshold, or `N days since review` when the last applied audit has aged out. The label tells you what accumulated and how much before you run the audit. (#589)
- amends the unreleased cost-contract data GAIA's dashboard reads: every new `cost.jsonl` record now carries `session_cwd`, ledger titles (specs `intent`, plans `subject`) use a shared first-sentence/word-safe-bounded rule instead of truncating mid-word, and plan rows gain a `status`/`completed_at` lifecycle through a new guarded chokepoint. This repo's archived `cost.md` history is backfilled into `cost.jsonl` and existing broken ledger titles are repaired. The `token-rates.json` read contract is now documented on the `Token Cost Readout` wiki page. All additive, no `schema_version` bump; pre-release, so no adopter-migration burden. (#578)
- `/gaia-debt` drain now runs the happy path start to finish like `/update-deps`: once the candidate is chosen it implements, gates, commits, opens the PR, and drives straight to merge with no separate merge-time confirmation. It still pauses only for the up-front candidate pick (two or more issues) or a genuine blocker (a security-class diversion, a rejected push, a gate that won't go green); the deterministic severity-then-age ordering, one-issue-per-invocation limit, drain-time security screen, and marker handshake are unchanged (#577)
- `/gaia-plan` and `/gaia-spec` recognize Fable as a top-tier planning model alongside Opus. On Opus or Fable the workflow proceeds without prompting; on Sonnet or Haiku it offers Opus (recommended), Fable, or the current model. `/gaia-plan` pins the spawned planner accordingly; `/gaia-spec` gains a new pre-flight gate that, because its synthesis runs on the interactive main thread and cannot pin a subagent, stops with a `/model`-and-re-run instruction instead. (#573)
- GAIA's token-cost artifacts consolidate onto a durable, versioned, self-describing `cost.jsonl` ledger for an external cost dashboard, documented on a new `Cost Data Contract` wiki page with `token-tally.sh` as its single source of truth. Also adds a terminal `abandoned` spec status with a guarded empty-draft sweep, so a never-authored draft stops resurfacing for resume. Pre-release, so no adopter-migration burden. (#569)
- the machine-local cache consolidates onto a single `.gaia/local/cache/` tree: the standalone `.gaia/cache/` is eliminated and its shared artifacts (update-check, the coaching flag, serena-guard, and `/update-gaia` tarballs) move to a worktree-symlinked `.gaia/local/cache/shared/`. Orphaned SPEC-workflow and react-perf artifacts (`gate1-*.json`, `renders.json`) are now reaped, on SPEC merge/close and by a 14-day SessionStart age-sweep backstop, and the `/update-gaia` tarball prune no longer deletes `serena-guard/` on every run. **Action required:** after updating, delete the now-dead cache directory once per clone: `rm -rf .gaia/cache` (its contents are regenerable runtime state). (#567)
- `/gaia-plan`'s pre-flight isolation prompt now offers the git-worktree option without the experimental caveat, since the worktree lifecycle is dependable as of #557. When a plan starts from a non-main branch, a worktree cut from main is now the sole isolation mode, so the concurrent-orchestrator scan and its three-option "Concurrent plan" prompt are folded away. (#566)
- `/gaia-debt` drain now presents the candidate choice through a structured `AskUserQuestion` prompt rather than a free-text override: it offers the top three backlog candidates (recommended one first) plus the built-in Other entry for a lower-ranked issue number, and skips the prompt entirely when only one `tech-debt` issue is open. The deterministic severity-then-age ordering, one-issue-per-invocation limit, drain-time security screen, and merge handshake are unchanged (#564)
- SPEC-number allocation moves from the tracked `.gaia/specs.json` ledger to immutable `spec/NNN` git tags on the remote (SPEC-021): the next number is max+1 over the union of the remote's `spec/*` tags and local signals, and a non-force tag push is the cross-machine collision lock, so a shared team no longer depends on everyone committing, pushing, and pulling a working-tree file in time. The ledger survives only as a local, gitignored per-machine cache at `.gaia/local/specs/ledger.json`. A clone without a populated `.gaia/specs.json` (i.e. a fresh scaffold) needs to do nothing: the allocator creates the local ledger on the first `/gaia-spec`, and the first online allocation seeds the remote tag namespace. **Action required:** none — `/update-gaia` performs the one-time ledger cutover for you automatically on your next update.
- a plan derived from a SPEC is colocated inside its SPEC folder at `.gaia/local/specs/<SPEC-ID>/plan/` (`plan-2`, `plan-3` for a revision) instead of living under `.gaia/local/plans/`, so plan-to-SPEC discovery is structural and the plan's merge-time disposition now follows its SPEC's. Applies to plans generated or merged after this lands; existing on-disk plan folders are not migrated (#544)
- adopt `@gaia-react/lint` 1.9.0 (from 1.8.0), which adds the `no-zod-enum` guardrail: `z.enum(...)` is now an ESLint error, use `z.literal([...])` for string unions (sorted alphanumerically). This makes the Zod convention a deterministic lint failure instead of a discretionary review note. **Action required:** convert any existing `z.enum([...])` schemas in your app to `z.literal([...])` to keep `pnpm lint` green (#540)
- plan-execution token and wall-clock accounting now records execute cost from the orchestrator's own git commits and pushes through a `PreToolUse` hook instead of a manual, instruction-driven tally, so no phase is double-counted; it aggregates the whole execution across every session and dedups re-invocation rows on read. A new merge-time `PostToolUse` hook renders a full-cycle spec / plan / execute / total cost breakdown at `gh pr merge` (the spec line omitted for a spec-less plan), and a new `.gaia/scripts/token-rollup.sh` reader surfaces the same roll-up on demand (#539)
- `/gaia-plan` now pins each plan-execution task sub-agent to Sonnet by default instead of letting it inherit the orchestrator session's model (typically Opus). The feature's complexity is resolved upstream in `/gaia-spec` and `/gaia-plan` and their audits, so execution runs faster and cheaper on the lighter model; the planner can still escalate a specific deep-synthesis phase to Opus, naming which and why in `ORCHESTRATOR.md` (#538)
- `/update-deps` completes the flow on a `main`/`master` run: it now writes the update commit with a load-bearing `chore(deps)` subject (previously Phase 8 referenced "the update commit" but never wrote one), opens the PR, and merges it once the required checks are green (`--auto` under branch protection), then verifies the terminal `MERGED` state and cleans up the local checkout. A run on any other branch (or in CI) is unchanged: it pushes and leaves the PR to the branch owner. The `chore(deps)` subject clears the merge gate's dep-bump bypass, so the PR stays turnkey-mergeable without a code-review-audit marker (#534)
- GAIA's Serena code-search routing guidance is now language-agnostic: the advisory `code-search` rule nudges toward Serena's LSP-backed symbol tools for symbol queries in any language Serena indexes for the project (not only TypeScript, and no longer scoped to `app/`/`test/`), so an adopter who configures another language server gets the same routing. The enforcement guard stays deliberately TypeScript-conservative and tsconfig-gated, since a wrong hard-block on a non-TS search is worse than a miss while the rule only nudges (#533)
- `/gaia-debt` drives its fix PR through the standard PR Merge Workflow to completion instead of stopping after `gh pr create` for a human to merge by hand: it confirms intent (open-only or merge, defaulting to merge), then resolves the marker handshake, the maintainer-only CHANGELOG gate, the merge (`--auto` under branch protection, never `--admin`), and the post-merge verify-and-cleanup itself. The `code-review-audit` marker gate is unchanged, the skill drives the merge and never bypasses, fakes, or pre-empts it (#523)
- `/gaia-spec`'s closing handoff now prints a single copy-pasteable block: the `/gaia-plan` command and its SPEC argument together, prefaced by a `/clear`-and-paste instruction, instead of a bare argument the user had to prefix with the command by hand (#519)
- catch the README up with shipped changes: correct the tech stack to React Router 8 and the Node engine floor to `>=22.22.0` (both moved with the React Router 8 major), and document `/gaia-debt` in the commands table and the tech-debt section (#517)
- `/setup-gaia` gates the Claude GitHub App install before its CI verification run: because every GAIA CI workflow authenticates as that app, the command now walks you through installing it (skipping the prompt when it detects the app already present on your org) and waits for confirmation, instead of dispatching a run that fails with a 401 and only then telling you what was missing. You can also skip verification and finish setup, then trigger a run from the Actions tab once the app is installed (#516)
- `/setup-gaia` finalizes greenfield CI setup by committing GAIA's own CI-install workflows directly to the default branch (no PR and no audit, since that commit has nothing to review), so first setup no longer waits on or wedges behind an audit run; the direct push accounts for repository rulesets and recovers cleanly when one blocks it, and the block-main guard stands down only for that single commit via a self-healing, gitignored sentinel. Its audit-mode onboarding now opens with a solo-or-team choice: solo engineers run audits locally by default (CI kept as a failsafe), and teams get a single Local-recommended-or-CI choice (#515)
- `/setup-gaia` now asks where a new GitHub repo should live, your personal account or one of your organizations, instead of silently creating it under your personal namespace; organizations are offered ranked by your own recent commit activity, and the prompt is skipped entirely when you belong to none. `/gaia-init`'s closing message drops the stale "create your GitHub repo, then run /setup-gaia" step now that repo creation lives inside `/setup-gaia` (#512)
- onboarding is now a single `/setup-gaia` command, replacing `/setup-gaia-ci` and `/setup-cloned-gaia-project`. It detects your situation (fresh clone, first adopter, partial run, or fully provisioned), runs only the phases still owed, provisions the GitHub repository (private by default), and is safe for any teammate to re-run after cloning. CI-token setup is out-of-band and never asks you to paste a secret into the chat: it reuses a `CLAUDE_CODE_OAUTH_TOKEN` or `ANTHROPIC_API_KEY` the repo or org already exposes when present (by name only, never by value), otherwise you set the secret yourself and the command only verifies it exists. **Action required:** run `/setup-gaia` instead of the two retired commands; `/update-gaia` surfaces the removal of the old command files with a per-file confirm prompt on your next update (#478, #480, #509)
- `/gaia-init` setup-flow polish. It auto-detects the user's GitHub handle for `.github/CODEOWNERS` via `gh api user` instead of always writing the `REPLACE-WITH-YOUR-GITHUB-HANDLE` placeholder (on failure it keeps the placeholder and the required-follow-up warning). The i18n and CI prompts are polished, and `/gaia-init` and `/setup-cloned-gaia-project` strip React Doctor's bundled extras after install (standalone Actions workflow, commit hook, `doctor` package script, pinned devDependency), since GAIA already triggers react-doctor via the skill and the `code-review-audit` agent at `@latest` (#454, #507)
- `/gaia-fitness` (and `/health-audit`'s shared fitness bucket) now dispatch each judgment-auditor with an explicit coverage directive in its prompt (surface every candidate, including uncertain or low-severity ones; the Orchestrator's adjudication stage is the filter), so recall no longer rides on the auditor's own bar. Documented in `wiki/decisions/Claude Integration Fitness.md` (#486)
- adopt `@gaia-react/lint` 1.8.0 (from 1.5.1), reconciling every lint delta across the repo. The headline behavior change: `no-null-render` now autofixes `return null` to `return undefined` inside render functions only (loaders, actions, and utils untouched; structural `null` left intact), and a report-only selector flags the `.length && <JSX/>` numeric-0 leak. The bundled toolchain (`typescript-eslint`, `eslint-plugin-sonarjs`, unicorn) moves in step and the surfaced findings are resolved (#433, #441, #461)
- the `prepare` lifecycle no longer provisions Playwright browsers: `pnpm exec playwright install --with-deps` moves to a dedicated `pnpm install:browsers` script, so `pnpm install --frozen-lockfile` no longer triggers an `apt-get` that can fail behind a proxy or on a hardened runner. **Action required:** run `pnpm install:browsers` once to provision Playwright browsers locally if you run the e2e suite (`pnpm install` no longer does it for you) (#456)
- the fresh-scaffold visual baseline is now neutral and brand-free (SPEC-008): the `--color-claude-*` terracotta scale is replaced by a zero-chroma `--color-primary-*` scale so re-skinning is a few-value change, Google Fonts are dropped for system stacks, and `Layout` is simplified to a single `<main>` wrapper. A path-scoped `design-baseline.md` guard and a `Design System.md` stub keep Claude from reading the neutral baseline as a chosen design system. **Adopters who customized owned files** (`Header`, `Footer`, `GaiaLogo`, or the bundle) will see a migration impact in `/update-gaia`; those files are removed upstream (#455)
- routine dependency refresh across several waves: `@types/node` to 26.0.0 (types-only, runtime stays on Node 22), plus the `storybook` group, `@playwright/test`, `tailwindcss`, `vitest`, `@faker-js/faker`, `chromatic`, `happy-dom`, `nanoid`, and further minor/patch bumps. Transitive `undici`, the nested `vite` 7.x copy, and the `qs` security-floor override reached their patched floors or became redundant through the bumps, so those interim overrides were pruned and `pnpm audit` stays clean (#387, #413, #415, #452)
- upgrade React Router 7.18.0 → 8.0.1 across the family (`react-router`, `@react-router/*`), a low-risk "boring major": GAIA already adopted all five v8 future flags on v7 and imports everything from `react-router`, so router runtime behavior is unchanged at cutover and the now-invalid `future` block in `react-router.config.ts` is deleted. It also bumps `remix-utils` to 10.0.0 and `remix-i18next` to 8.0.0 (both peer `react-router` ^8 natively), and the nested `vite` 7.x toolchain copy disappears because `@react-router/dev@8` widens its vite peer to ^7||^8, so the dev-only audit advisory has nothing left to flag (closes #398). **Action required:** raise your local Node to >=22.22.0 (the v8 engine floor; `.nvmrc`/`.node-version` pin 22.23.1 on the 22.x LTS line) (#444, #450)
- catch the README up for the next release: add `/gaia-react-perf` and `/gaia-harden` to the commands table, add `a11y-fixes` to the bundled-skills lists, and refresh the lint-rule count (#449)
- replace the handoff archive lifecycle with delete-on-done. `/gaia-handoff` enforces a single-handoff invariant (it deletes any existing handoff before writing, carrying forward anything unfinished), and `/gaia-pickup` resolves it in place, self-deleting on the happy path and leaving outstanding work untouched so an interruption stays recoverable, instead of moving consumed handoffs into an `archive/` directory. Deletion is the only terminal state; nothing is archived. **Action required:** remove any orphaned archive directory left by the prior workflow with `rm -rf .gaia/local/handoff/archive` (gitignored local scratch, safe to delete) (#448)
- split the spec-session lifecycle from the SPEC artifact's status: the spec ledger tracks the authoring session while `SPEC.md` frontmatter keeps its own (`in-progress | reopened | closed`). `/gaia-spec` resume only offers a still-`draft` session, so a finalized SPEC is never re-surfaced as resumable, and a fail-open reconcile pass derives the `merged` transition from a merged PR on git ground truth (#423)
- route GAIA workflow-misfire reports (hooks, skills, commands, quality gate, scaffold failures) through `/gaia-forensics` before filing by hand, and document an issue-claiming policy in `CONTRIBUTING.md`: only issues labeled `help wanted` are open for outside PRs (#404)
- the add-locale runbook no longer adds new-locale files to `.gaia/manifest.json` (those files are adopter-owned), and the remove-i18n and `/update-gaia` manifest steps now write through an exemption marker so they still complete under the new guard. The guard registers in `.claude/settings.json`, a shared, non-field-aware file, so it activates once that file merges cleanly; a drifted adopter must resolve the settings conflict first

### Removed

- the orphaned `setup-ci set-secret` CLI verb, superseded by #478's out-of-band token provisioning: dead-but-advertised surface and a latent paste-reintroduction vector (invoking it required an agent to hold the CI bot token on stdin). Deleted outright; maintainer CLI surface, so release-excluded (#479)
- `app/components/Header/`, `app/components/Footer/`, `app/components/GaiaLogo/`, and `app/assets/images/gaia-logo.svg` from the template: these GAIA-branded surfaces are removed from the scaffold rather than shipped and stripped at init, with the neutral index + `Layout` in their place. Adopters who customized any of these owned files will be offered a migration note by `/update-gaia`; the maintainer logo the README references was relocated to `docs/assets/gaia-logo.svg`, outside `app/` so it never enters the app build or the distributable (#455, #458)
- the vestigial `react-router-dom` dependency, a v6/v7 re-export shim that React Router 8 drops entirely; GAIA runs framework mode and imports everything from `react-router` (`HydratedRouter` comes from `react-router/dom`), so it was already dead weight. `/update-gaia` leaves an existing `react-router-dom` in your `package.json` by design (adopter-owned dependencies are never auto-removed); `pnpm knip` also flags it as unused (#419)
  - **Action required:** run `pnpm remove react-router-dom` to drop it
- the `/gaia-spec` GitHub-issue mirror and the inline `/gaia-plan` chain (SPEC-012). The mirror is gone end to end (the opt-in question, auto-mode force-on, resume re-ask, mirror step, PR-body-closure, frontmatter stamping, and the `gh-mirror.sh` script). The chain is replaced by a handoff: after the canonical save `/gaia-spec` prints a copy-pasteable `/gaia-plan` handoff prompt and stops, so the human runs `/gaia-plan` in a fresh session with a clean context budget (#484)
- GAIA's adaptive mentorship layer and the structured event pipeline that fed it (`gaia mentorship enable|disable|status|purge`), an opt-in, local-only feature that adapted Claude's responses to observed dev-workflow patterns over time. It shipped off by default, nothing in the shipped surface read the stream it wrote or transmitted it anywhere, and it charged every new adopter a blocking privacy decision during `/setup-gaia`'s first run before they had any signal the feature was worth the ask. The four retired commands are `gaia mentorship enable`, `gaia mentorship disable`, `gaia mentorship status`, and `gaia mentorship purge`: drop any script that calls one. **Action required:** none — a one-time SessionStart janitor sweep destroys the mentorship residue on your next session start, including the cloud event stream, which accumulated regardless of whether mentorship was ever opted into. A locally drifted setup document that still passes `--mentorship` to `gaia ping` will fail that command's flag parse.

### Fixed

- the distribution suite only exercised `gaia init` against the real staged release tree; every other bundled subcommand had zero staged-artifact coverage, so a `release-exclude`/scrub regression on a shipped template or CLI code path could ship silently, with `run-all.sh` staying green. Two new adopter-flow scenarios close part of that gap: one runs all four `gaia scaffold` kinds and asserts the shipped templates render correctly, the other runs the `gaia setup` subcommands behind `/setup-gaia` and asserts their JSON contract on a scrubbed binary. `gaia setup-ci`, `sandbox`, `ping`, `wiki`, and `update`/`update-deps` remain uncovered. Maintainer-only: `.gaia/tests/` is release-excluded, so no adopter clone is affected (#672)
- the manifest-write guard hook (`.claude/hooks/block-manifest-write.sh`) inspected only the first line of a command, so a write to `.gaia/manifest.json` on a later line of a multi-line command, or one using a no-space redirect (`>.gaia/manifest.json`), slipped past the guard entirely. The guard now tokenizes every line of the command and recognizes the glued redirect-target shape, closing the bypass while keeping its allow-on-ambiguity bias. Defense-in-depth hardening only; the OS-level filesystem sandbox remains the airtight protection for the manifest (#852)
- the release scrub reported 20 distribution-boundary leaks on `main`: a dangling `UAT-NNN` narrative reference, and maintainer-only paths and `code-audit-maintainer-*` agent names left unwrapped by the recent audit-machinery work, hard-exiting the distribution suite at the scrub step before a single scenario ran. Incidental mentions are reworded where the surrounding prose is adopter-relevant; genuinely maintainer-only entries (internal build-source paths, maintainer-only agent names, the release-excluded `resolve-audit-spawn.sh`) are now wrapped in `gaia:maintainer-only` markers. All post-v1.6.1 regressions; nothing shipped. Maintainer-only tooling, so no adopter clone is affected (#847, #849)
- creating a worktree whose name collides with a concurrently-launching session's no longer risks deleting that peer's in-flight checkout. Worktree creation now serializes per name, so a losing race only ever inspects a fully settled peer and leaves it alone, closing the residual timing window the earlier collision fix (#759) could not, since any inspection-only check is a snapshot. The same change self-heals a worktree left half-registered by a session killed mid-creation, which previously wedged that name until it was cleaned up by hand (#762)
- `/gaia-plan` task sub-agent dispatch now carries the same fail-loud tree self-check the pre-merge Code Audit Team dispatch got (#765/#797): a task's first action verifies its working root's tree matches the orchestrator's freshly-captured expectation and stops with no edits on any mismatch, closing a gap where a stale or wrong worktree root could let a task silently edit the wrong tree (#798)
- harden the Code Audit Team merge gate against three latent gate failures plus two smaller defects. The `GAIA-Audit` trailer stamp is serialized behind a portable mutex, so parallel Code Audit Team members finishing near-simultaneously on a multi-member diff can no longer double-commit or wedge on `index.lock`. The CI trailer check now reads the newest `GAIA-Audit` status per context and requires that newest entry to be success, matching the local merge gate, so a newer failed or pending re-run shadows an older success instead of a stale success clearing CI. A pre-merge audit dispatched from a git worktree now reviews that worktree's own tree and hard-stops with no marker if pointed at the wrong tree, closing a silent fail-open where a worktree audit could pass on the main checkout's empty diff. The local success status also surfaces the sha it actually posted to (the pushed PR head) rather than local HEAD. Maintainer-only: this machinery is release-excluded, so no adopter clone is affected (#765, #792, #794, #795, #796)
- the Code Audit Team merge gate now requires every dispatched member to clear the audited tree before the shared `GAIA-Audit` trailer is stamped, so one member's clean pass can no longer stand the whole gate down on a diff that dispatched more than one member. The required `GAIA-Audit` status now posts to the pushed PR head commit (the one branch protection checks) instead of an unpushed local commit, so it reliably lands on the empty-commit stamp path, and the local merge check reads the latest status per context so a later failure supersedes an earlier success (#726)
- the builtin fallback audit roster now grants `code-audit-maintainer-shell` the same five declarative machinery surfaces the committed `.gaia/audit-ci.yml` roster does (`.gaia/audit-ci.yml`, `.gaia/VERSION`, the `code-audit-*` agent definitions, `.claude/rules/**`, and the bundled `code-review-audit.yml` template). That fallback is consulted only when `.gaia/audit-ci.yml` is absent or its `auditors:` block is unparseable, and in that degraded state a change to one of those surfaces was ownerless, so the merge gate dispatched nobody and would have merged it unaudited. A regression test now enforces that every machinery path stays roster-claimed under the fallback roster too. Maintainer-only: the roster entry and test are release-excluded, so no adopter clone is affected (#786)
- file-write `deny` rules in `.claude/settings.json` written as `Write(<glob>)` were silent no-ops: Claude Code matches file mutations only through `Edit(<glob>)` rules (which already cover Write, Edit, MultiEdit, and NotebookEdit), so a `Write(...)` deny never fired. Most consequential, the audit-clearance sentinel guards (`.gaia/local/audit/*.ok`, `*.refused`, `*.carried`) were unenforced, so any tool could write a sentinel and fabricate an audit pass; they are now `Edit(...)` deny rules that block every file-writing tool. The redundant `Write(.env)` and `Write(pnpm-lock.yaml)` denies (already covered by their `Edit(...)` twins) and the four no-op `Write(.gaia/local/**)` allow rules (covered by `Edit(.gaia/**)`) are dropped (#783)
- the code audit's progress-breadcrumb log (the CI step-summary observability file) was a single fixed path shared by every concurrent GAIA session on a machine, since the drop-zone it lives in is symlinked into every linked worktree. Two sessions auditing at once could overwrite each other's breadcrumbs, and a reader, human, orchestrator, or the public Actions step summary, could attribute a peer session's audit run to their own with no signal it wasn't theirs. The log is now keyed to the audited tree, the same way the merge-gate marker already is, so concurrent sessions never collide (#772)
- the bats suites guarding GAIA's own framework bash are now audited before merge. They were owned by no Code Audit Team member: every member's globs match `*.sh`, a `.bats` file is not a `.sh` file, so a pull request touching only test suites dispatched nobody and rode the merge gate's out-of-scope bypass to merge unreviewed. Those suites are the only enforcement standing behind the framework's shell, which made a commit that weakens, skips, or deletes one both the least affordable to merge unaudited and the easiest to slip through. `code-audit-maintainer-shell` now owns them (`.gaia/**/*.bats`, `.github/**/*.bats`, covering every bats tree in the repo, not only the one the report named), and the agent gains a lens for the failure modes a suite's own green run cannot catch: an assertion that structurally cannot fail, a silently loosened or deleted assertion, and a `skip` that retires coverage. Maintainer-only: the roster entry and agent are release-excluded, so no adopter clone is affected (#769)
- creating a worktree whose name collides with one you already have no longer destroys the existing worktree. A collision makes worktree creation fail, and the failing hook's cleanup step force-removed the target path without checking whether that run had created it, so it deleted the worktree already sitting there, along with any uncommitted work in it, and then still reported failure. The cleanup now removes only a path its own run created, and tells you it left the other one alone. An existing worktree is protected whether it was on disk before the run or a concurrently-launched session created it (#759)
- the CI audit workflow's out-of-scope skip and local-mode stand-down resolved their target sha from `git rev-parse HEAD` instead of the pull request event payload the other status writers already use, leaning on an implicit invariant (no commit-creating step precedes either) rather than an explicit one; both now read the sha from the event payload, matching the rest. The same skip step's non-clobber guard could also stand down on an unreadable status read with nothing published for that case, so its terminal PR comment fell through to asserting the gate was "pending, not green" without ever having verified that; a new output and comment branch now tell the author the gate's state is unknown instead. The step's `status_posted` output is also renamed to `success_stamped`, since it read `false` on the members-pending exit even though a `pending` status had in fact been posted (#723, #742, #743)
- the merge gate's non-clobber guard (`.github/audit/audit-success-present.sh`) could report no `GAIA-Audit` success was live for a commit that had one, since a pipe into `grep -q` under `set -euo pipefail` risked a SIGPIPE'd upstream write flipping the match into a false "not live" exit. Unreachable in production today (the endpoint it reads returns at most one short status line, well under the pipe buffer), but the trap stayed armed for a future change to that read. Replacing the pipe with a here-string removes the hazard (#745)
- the pre-merge audit procedure now tells you to run the self-healing auditor first and alone, rather than spawning every dispatched Code Audit Team member at once. Markers are keyed to the tree, which makes the members order-independent and parallel dispatch safe in general, but the frontend auditor is the only member that edits files, and a self-heal is a real content edit: it produces a new tree and so invalidates every marker for the old one, including the ones its siblings are in the middle of earning. Dispatching the whole roster in parallel therefore throws that work away and forces a full re-spawn whenever a self-heal lands mid-flight. Plain parallel dispatch still applies when the frontend auditor is not in the spawn set, or the diff is one it refuses to self-heal by design (#747)
- the pre-merge audit now spawns the Code Audit Team members the diff actually dispatches, instead of always spawning the frontend auditor and no one else. The roster and its merge gate were already correct, but the instructions that decide *who to spawn* named a single auditor, so a diff with no frontend changes still paid for a frontend review, and a diff owned by a specialized member spawned nobody at all, leaving the author to discover the requirement only when the merge was denied. A new spawn oracle (`.gaia/scripts/resolve-audit-spawn.sh`) answers "who must audit this diff?" in one deterministic call, every pre-merge surface now spawns exactly the members it names (zero, one, or several), and the frontend auditor stands itself down when the diff is not its own. **Action required:** none for adopters, though `/update-gaia` will offer one new `.claude/settings.json` allow entry, `Bash(bash .gaia/scripts/resolve-audit-spawn.sh:*)`, which lets the merge procedure run the oracle without prompting (#736)
- a PR reviewed only by an auditor CI cannot run could never clear its own merge gate. CI and the local auditor both write the `GAIA-Audit` commit status, a status has no compare-and-set, and CI's "members pending" write frequently landed last, overwriting the success the local auditor had just posted. The check reverted to pending, branch protection refused the merge, and nothing re-posted, so the only way through was to notice the overwrite in the status history and re-run the status helper by hand. CI now reads before it writes: every step that posts a pending status leaves a live success for the current tree alone, including the local-mode stand-down that fires on the ordinary path where CI defers the audit to the author's machine. It still fails closed in every other direction, both for a success naming a stale tree and for a status it cannot definitively read (#734, #736, #739)
- the `rm -rf` guard denied a **path-qualified** command word's target, and blamed a path the user never wrote. `/bin/rm -rf dist` was blocked even though `dist` is explicitly whitelisted, with the deny text naming an absolute path `/rm` that appears nowhere in the command: the guard anchors each segment at the `rm` word, so the `/` in front of the word became the segment's leading byte and the resulting `/rm` token was judged as though it were the operand. It failed in the safe direction (a false deny, never a bypass), but it was inexplicable from the message, and it also meant the genuinely dangerous spellings were denying for the wrong reason, tripping over `/rm` rather than over the real target. The command word is now skipped by its **position** in the segment, where the anchor guarantees it sits, rather than by its spelling: `/bin/rm -rf dist` and `/usr/bin/rm -rf dist` pass, the dangerous shapes still deny and now deny through the target's own arm, and the `rm` binary named as a target (`rm -rf /bin/rm`) stays denied as the absolute path it is, which a spelling-based skip would have quietly turned into an allow. The hook also gains a `main` entry point so it can be sourced by its suite: the quoted-separator walk is position-preserving, which is the entire reason the guard may skip that walk for a command holding no `rm`, and no end-to-end payload can tell a correct walk from a broken one, so that property is now asserted directly on the helper instead of resting on a comment (#715, #724)
- a multi-member code audit could never clear its own merge gate on the natural path. Code Audit Team markers were keyed to the HEAD commit sha, but `code-audit-frontend` stamps the `GAIA-Audit` trailer, and on an already-pushed HEAD that stamp lands as an empty commit: it advances HEAD while leaving the tree byte-identical. Every sibling member's marker was orphaned the instant it landed, so on a diff that dispatched more than one member, all members could audit the identical tree and the gate would still block, reporting the others as pending. It failed closed, so nothing unsafe merged; the cost was that the obvious move, running the members in parallel, always lost, and the only way through was to run the frontend first and alone and then re-run every other member against the post-stamp HEAD, which nothing documented. Markers are now keyed to the tree sha instead. A marker attests that a member reviewed content, and in git the tree is the content, so the trailer stamp no longer invalidates anything and the members can run in any order. The gate is not loosened: a commit that genuinely edits the tree still invalidates every marker for the old one, which is the re-audit the gate exists to force. The local janitor learned the new key in the same change, since it reaps a marker whose key names nothing live and would otherwise have deleted every tree-keyed marker on its next run (#709)
- the `rm -rf` guard hook (`.claude/hooks/block-rm-rf.sh`) guarded `.git` but not `.claude`, and recognized only the literal lowercase spelling of the `rm` command word. `rm -rf .claude` was **allowed**, which removes every hook, skill, agent, rule, and setting, while `.git` sitting directly beside it was denied and the guard's own deny text for the dotfile glob already named `.claude` as a thing worth protecting. Separately, the command word was matched as a case-sensitive, contiguous `rm` substring even though the *target* half of the guard normalizes properly (it strips quotes and backslashes, which is what catches `rm -rf "$HOME"`), so any spelling bash still resolves to `rm` exited the hook before a single deny arm was reachable and carried every protected target out with it: `RM -rf ~` really deletes home, because macOS mounts case-insensitively by default and `/bin/RM` therefore is `/bin/rm`, as do the quote- and backslash-split forms `r""m`, `"r"m`, `r"m"`, `'r'm`, and `r\m`. Both are closed: `.claude` denies alongside `.git`, and the command word is matched the way bash resolves it, case-insensitively and through quote and backslash removal. Ordinary cleanups still pass, including neighbours like `.claudia`, a word that merely contains `rm` such as `charm -rf x` or a commit message mentioning a "warm restart", and every whitelisted scratch path. Note that a recursive removal of `.claude`, or of any path under it, now denies: removing a worktree or a cached artifact there goes through `git worktree remove` or a deliberately scoped command instead (#713, #714)
- the CI audit workflow could tell a PR author the merge gate was satisfied when it had stamped no status at all. On a PR whose un-audited delta touches nothing the auditor owns, the workflow stamps a `GAIA-Audit` commit status so the merge clears without a ceremonial local audit run, but when `.gaia/VERSION` is missing or empty (a botched merge, a partial checkout) it bows out having posted nothing, and the terminal comment announced the free skip regardless. The gate itself fails *closed*, an absent required check shuts the merge button, so this was misdirection rather than a bypass: the author was told the button was open while it was shut, and the only truthful signal was buried in the step log. The comment now reports whether the status actually landed, and when it did not it says the gate is unsatisfied and names the likely cause. It is the same green-sounding-comment-over-a-closed-gate class as the member-aware fix before it, reached through the version guard instead (#711)
- the release scrub's leak-check for maintainer-only Code Audit Team member names skipped `.github/audit/**`, the one shipped directory whose scripts name audit members by design and so the likeliest place for a maintainer-only member name to reach an adopter. Its sibling leak-check for maintainer-only paths already scanned that directory as "exactly where a maintainer-only path reference would most plausibly leak", so the omission was an oversight rather than a deliberate exclusion. Nothing leaks today (the shipped gate script names only the auditor CI runs), so this closes a hardening gap rather than a live defect; the scrub gates the build instead of rewriting content, so the bytes an adopter receives are unchanged (#710)
- the code audit's security screen was specified so that it either did nothing or blocked everything. A finding carrying "no stable `finding_class`" counted as security-class and could never be filed to a public issue, but `holistic/unclassified`, the fallback class every out-of-scope finding receives when it maps to none of the deliberately-small seeded vocabulary, is precisely that classless state. Read literally, every out-of-scope finding on a public repo had to divert, so the `tech-debt` backlog would always be empty and `/gaia-debt` could never fix anything; read loosely, the trigger was ignored and a genuine security finding's never-public guarantee rested on model judgment rather than on the stated rule. The screen now keys on a finding's content and severity and never on its class: the fallback class is the expected verdict for most out-of-scope findings and carries no security signal, so it is not a trigger, while a genuinely absent or malformed class field still diverts as a broken finding record. `/gaia-audit` had already carved out this exception for its own findings; that carve-out becomes the root rule instead of a local workaround, so the agent, `/gaia-audit`, and `/gaia-debt` now screen identically (#698)
- the release scrub failed against GAIA's own source tree, which broke the distribution harness, the only end-to-end oracle for what actually reaches an adopter. Every harness that stages a tree died in the scrub phase before reaching a single assertion, so the suite reported the same red whether the distribution boundary was correct or catastrophically wrong, and it had been silently broken for everyone running it. The cause was the obscurity rule that keeps the mentorship telemetry location out of shipped surfaces: it matched any `events-<date>.jsonl` filename, but the separately-consented cloud telemetry stream is written by the same NDJSON writer and shares that filename convention by design, so the rule flagged the cloud reaper's own functional code as a leak. The filename shape is shared convention; the directory is the thing worth hiding. The rule now anchors on the mentorship directory instead, which is net *tighter* on the axis that matters, since any mention of that directory now trips it rather than one exact path spelling. Two shipped surfaces that named release-excluded maintainer-only paths are reworded alongside it. The scrub's leak-check gates the build rather than transforming content, so the bytes an adopter receives are unchanged (#716)
- the `rm -rf` guard hook (`.claude/hooks/block-rm-rf.sh`) allowed `rm -rf .*`, which removes `.git` and `.claude`. The dotfile glob sat precisely between `rm -rf .` and `rm -rf *`, both already denied, so the one shape here that is plausibly an *accident* rather than evasion was the one that got through: anyone reaching for `.*` is trying to clear dotfiles, which is exactly when `.git` is what they least want to lose. The same hole covered the `.[!.]*` and `..?*` idioms people reach for to skip `.` and `..`, the brace form `{.,}*`, the `$PWD` spellings of an already-denied target, and `~user`. Separately, a quoted `;`, `&`, or `|` inside an operand truncated the guard's command splitting, so every operand after it went uninspected and `rm -rf ";" $HOME` deleted home unchallenged. Both are closed, and the guard still lets ordinary cleanups through: a glob nested inside a whitelisted path, `$PWD/dist`, and scoped brace lists all remain allowed. One known false-deny stays on purpose, and the hook header now explains why: the guard denies commands that merely *mention* a target inside a quoted string (a commit message describing this fix, for instance), because the change that would fix it would also let `bash -c`, `ssh host`, and `eval` forms smuggle a real deletion past the guard. Deliver such text via `--body-file` / `-F` or a variable (#712)
- the `file-tech-debt` skill left a scratch file behind on every filing. It wrote each issue body to a file under `.gaia/local/audit/` so the body would never reach `argv` (where a `--verbose` CI run would echo it into a public log), but never deleted it afterward, and no janitor sweep matched the leftovers, so they accumulated in the working tree indefinitely. The recipe now deletes the body-file once the create returns, whether it succeeded or failed, and gives each run its own uniquely-named file so a concurrent CI-plus-local pair cannot delete each other's in-flight body (#708)
- the `rm -rf` guard hook (`.claude/hooks/block-rm-rf.sh`) missed most of the ways a dangerous command is actually written, and reliably denied only the sloppiest spelling of each. Every one of these was **allowed**: a quoted target (`rm -rf "$HOME"`), the brace form (`${HOME}`), a backslash escape (`rm -rf \/` handed *root* to rm), a target on a line continuation, an operand written before the flags (`rm $HOME -rf`, which GNU rm permutes and deletes home all the same, so it is live on Linux while looking harmless when hand-tested on macOS), and a dangerous target in any but the first rm of a chain (`rm -rf dist && rm -rf /`). `--no-preserve-root` was also unreachable when written ahead of the recursive flag. The common thread is an inversion worth naming: the guard matched the careless spelling of a target and missed the deliberate one, so writing *good* shell made you more likely to slip past it. The fix is to normalize a target the way bash does, stripping quotes, escapes, and continuations before matching, and to inspect every rm in a command rather than the first. This is defense-in-depth rather than an open door, `settings.json` pre-approves only narrow scratch-path `rm` forms, so a bypassing command still met a normal permission prompt; the missing second layer is what is restored. The hook's header now states plainly that its deny list is a floor and never a ceiling, and names what a text matcher cannot see (computed targets, command substitution, `xargs`, a missing `jq`) along with the literal shapes still open (tracked as tech debt). It also gains the table-driven bats suite it never had, 74 deny/allow cases asserting that each target denies for the *right* reason; the suite and an adversarial audit found six of the seven gaps, only one having been filed (#699)
- `/gaia-debt` no longer recommends batching unrelated issues that happen to sit in the same directory. Its clustering rule pairs issues that share a finding class and a directory, but every filed issue currently falls back to the same unclassified sentinel, so the class half of the test was always true and the rule silently degraded into directory-only matching, which the skill itself rejects as too weak to batch on. It offered a five-issue batch of unrelated hook fixes as a result. The rule now requires a real seeded class, so it stands down while the backlog is uniformly unclassified, related issues still batch by shared file, and directory-level batching resumes on its own once classes are seeded (#707)
- the wiki-style rule's own audit block could not be run as written. Its historical-phrasing grep ended an alternation in a bare `set`, which the env-dump guard reads as a bare environment dump and denies, so an agent auditing a `wiki/**` or `.claude/skills/**` edit got a permission error instead of grep results. The check silently never ran while the rule read as though it had, which is the worst way for an audit to fail. The grep now matches the same strings without tripping the guard, and all five of the rule's audit commands execute end to end (#704)
- `.claude/hooks/block-spec-plan-chain.sh` shipped to adopters but was missing from the manifest, so `/update-gaia` could not see it: an adopter received the hook once at scaffold and never got a later fix to it. It is now registered as `owned`, like the other hooks, and updates normally. This was the first file the new distribution gate caught (#686)
- the spec-kit version check's uvx fallback could stall `/gaia-spec` on a degraded network. That fallback resolves the runtime version by fetching the pinned spec-kit ref, and it runs inside the mandatory `before_specify` gate, so on a captive portal or slow DNS a cache-miss run hung with nothing bounding it. Both of the call's network surfaces are now bounded by their own tooling's knobs rather than a `timeout(1)` that macOS does not ship: uv's HTTP read timeout, and git's low-speed abort, which is the one that matters, since uv fetches a `git+https` ref by shelling out to the system git where its own HTTP timeout never reaches. Neither is a hard wall-clock cap and the script says so; operator-set values win. The uvx route also gains the `specify version` second chance the `PATH` route already had, so a future pinned spec-kit that exposes only the bare subcommand resolves for uvx users instead of blocking them (#677)
- the spec-kit version check could not see a uvx-run spec-kit, so `/gaia-spec` was blocked for anyone who installed spec-kit the way GAIA documents. The check resolved the runtime version only from a `specify` on `PATH`, but GAIA's install route is uvx-mediated (`uvx --from git+…@vX.Y.Z specify`) and never puts one there, so a correctly installed, in-pin spec-kit reported as unresolvable drift and the mandatory `before_specify` hook halted. It now falls back to the uvx route before failing, and its day-cache (which never actually short-circuited) now works, so the fallback resolves once a day rather than on every hook (#676)
- the release pipeline never verified that the committed `.gaia/cli/gaia` and `gaia-maintainer` binaries actually reflect current `src/`, only that they exist and are executable; a maintainer who edited CLI source and forgot `pnpm bundle` could ship a stale binary with nothing to catch it. `release.yml` now rebuilds both binaries pre-publish and fails the release loudly if either differs from the committed bytes. Maintainer-only release tooling, so no adopter clone is affected (#641)
- a comprehensive-audit false-positive class and three health-audit runbook/taxonomy documentation gaps, all surfaced by the same audit run. Bucket B's source-tree grep triage had no mechanical completeness gate, so an incomplete enumeration could default every un-enumerated line to a genuine finding (one run produced 49, then 29, false findings); it now requires the triaged-line count to reconcile against the raw grep output before reporting a verdict. Bucket E's fitness categories are now pinned to the repo root so a monorepo layout can't pull in a sibling directory's `CLAUDE.md`, the marker-strip skip rule in both the runbook and the taxonomy now names all three configured transforms instead of only the HTML-comment one, and the Fixer lanes table gains `.gaia/scripts/**` under the `source-ts` lane. Maintainer-only and release-excluded, so no adopter clone is affected (#665)
- a live distribution-boundary leak, and a release-blocking manifest staleness. Two shipped `.gaia/scripts/` scripts referenced their release-excluded bats suites in unwrapped comments, and neither leak-check primitive's scope reached `.gaia/scripts/` or `.github/audit/`, so a shipped script could point at a callee absent from every adopter clone and no check would say so. Both scopes now cover those trees, the references are marker-wrapped, and the runtime-dependency check no longer mistakes a reference to a shipped directory for a leak. Separately, `release manifest --check` (a hard CI gate) was failing on a stale manifest and is clean again. Maintainer-only release tooling, so no adopter clone is affected (#663)
- the maintainer-only Comprehensive Audit phase's report-only invariant now catches a leaf mutating gitignored `.gaia/local/` state instead of only tracked files, closing the gap that let a refuter destroy a live SPEC folder by probe-running a destructive script unnoticed; refuters are now explicitly read-only. The invariant's inventory is scoped to the durable working-state directories it protects (`specs/`, `plans/`, `handoff/`) rather than all of `.gaia/local/`, so routine background churn (a debt-count refresh, telemetry appends, other audit runs) no longer false-flags as an integrity violation. Maintainer-only and release-excluded, so no adopter clone is affected (#663)
- a shipped wiki page, the Code Audit Team ADR, leaked maintainer-only content (the `code-audit-maintainer-*` auditors and `.gaia/cli/src/` references) unwrapped and was missing from `.gaia/manifest.json`; the content is now marker-wrapped and the page is registered (#663)
- three adopter-shipped trees now fall inside the bundle-time `maintainer-paths` leak check: the composite actions under `.github/actions/`, the CI issue-body templates under `.gaia/templates/`, and the spec-kit preset under `.specify/presets/gaia/`. A maintainer-only path reference landing in any of them would have shipped to adopters pointing at a release-excluded path absent from their clone, with no check to flag it. Additive scope on an already-enforced check, and all three trees are clean today, so no adopter clone is affected (#662)
- a tech-debt fix merged from inside a linked git worktree now refreshes the main checkout's `Run /gaia-debt` statusline nudge instead of leaving it frozen for hours. Linked worktrees shared five `.gaia/local/` state paths but not `debt/`, so a worktree merge armed the worktree's own throwaway debt-count cache and sentinel while the main checkout's stayed stale until the 6h refresh TTL; `debt/` now joins the shared-state symlink set (#640)
- maintainer-only surfaces under `.gaia/tests/` no longer point at a never-authored smoke-harness convention doc. Six references cited `.claude/rules/maintainers/smoke.md` as the routing rule for classifying a verification artifact by shape (release-gate harness vs UAT runbook), but the file was missing, so every pointer dangled; it is now authored as a concise, path-scoped maintainer rule that states the shape axis and defers the authoritative decision table to `.specify/extensions/gaia/rules/smoke.md`. Release-excluded, so no adopter clone is affected (#637)
- the `Run /gaia-debt` statusline nudge no longer lingers for hours after the tech-debt backlog empties. A tech-debt issue closed outside a `/gaia-debt` PR merge (a direct `gh issue close`, the web UI, a teammate, another session) dropped no refresh sentinel, so the stale open count survived to the refresher's 6h TTL and a restart just re-read the cached value. A direct `gh issue create`/`close`/`reopen` now refreshes the count alongside `gh pr merge`, and a SessionStart hook reconciles a stale-high count on the next restart (network-free when the backlog is already empty), so the nudge clears promptly (#598)
- a `/gaia-audit` apply run no longer orphans a machine-local memory file. Stage 2 removed a `MEMORY.md` index pointer before the `promote`/`delete` that removes the file it points at, so on a delayed `--apply` where that removal skipped on drift, the pointer was already gone while the source file survived, de-indexed with no path back to it. Index-pointer removals now run last and are gated on their referenced action having landed (via a new optional `depends_on` on `delete-entry`), so a skipped `promote`/`delete` leaves both the source file and its pointer intact (#593).
- a plan executed inside a linked worktree no longer records zero execution cost. The per-commit cost tally located the active plan by globbing for its `RUNNING` sentinel relative to the current directory, but a worktree never symlinks `.gaia/local/specs`/`plans`, so from a worktree the sentinel (which lives only in the main checkout) was invisible and every execute row was silently dropped. The plan search is now anchored to the main checkout, matching how the cost ledger already resolves. This matters because `cost.jsonl` is the sole durable cost record and a merged plan/spec folder is eventually pruned (at merge for a spec-less plan, or after the retention window for a spec-tracked one), so a dropped execute row was unrecoverable; ships with worktree bats regression coverage (#587)
- the GAIA-generated wiki and update-deps CI workflows no longer fail their auto-merge `git push` with `Invalid username or token` whenever the maintenance chain produces committable changes. A `claude-code-action` step runs in OIDC mode and rewrites the checkout-persisted git credential around a short-lived App token that no longer authorizes the push; the shared auto-merge partial and the update-deps wave-B push now reset the credential to the workflow `GITHUB_TOKEN` immediately before pushing. Runs that hit the "no changes" guard exited before the push and reported green, so the defect stayed hidden until the first run that produced real edits (#584)
- the `/gaia-plan` orchestrator template now spells out the explicit worktree-creation step its worktree-mode paths reference. The reframed pre-flight branch policy pointed both the on-main worktree option and the not-on-main path at a creation procedure that was never written down, so a cold orchestrator running in worktree mode had no concrete `EnterWorktree` call to follow and would hit the plan-folder-location and archive-guard traps unaided. (#568)
- the SPEC ledger mutex now lives beside the ledger it guards in gitignored `.gaia/local/specs/` instead of one directory up in tracked `.gaia/`. On Linux and CI (where `flock` is present) the lock created a real 0-byte `.gaia/specs.lock` that lingered after release and surfaced as untracked in `git status`, so a `git add -A` could stage it into the tree; macOS never saw it because the atomic-mkdir fallback removes its empty lock dir on release. Placement and hygiene only, no functional change (#563)
- the **Worktree** option for plan execution no longer aborts at creation: the `WorktreeCreate` hook read the selected worktree name from a field the harness does not send, so it failed with `missing worktree_name` every time. The hook now reads the name the harness actually provides and branches a fresh worktree under `.claude/worktrees/`, and a new paired `WorktreeRemove` hook makes teardown self-cleaning, deleting the worktree's branch and pruning the empty parent directories that previously lingered after removal. Both hooks ship with bats regression suites (#557)
- the `code-review-audit` CI workflow no longer reds a clean PR from its clean-no-push status step. That step stamped the `GAIA-Audit` commit status on the runner's `git rev-parse HEAD`, which by then can be a local, never-pushed commit the audit created during the run (an empty trailer marker or a refused self-heal edit), so the status POST hit a sha GitHub does not have, returned HTTP 422, and turned an otherwise-clean audit's required check red. It now targets the pushed PR head from the event payload (the sha GitHub is guaranteed to have) for the status, marker lookup, and tree, and treats the POST as non-fatal, so a failed side-effect can never red a clean audit (#532, #535)
- a `local`-mode author no longer triggers a redundant full audit in CI. The per-author-mode resolver confirmed the `GAIA-Audit` gate by reading classic branch protection, which needs repo-admin rights the Actions `GITHUB_TOKEN` never carries (and 404s on ruleset repos), so in CI it always failed closed to `ci` and ran the full audit in parallel with the author's authoritative local run. Under GitHub Actions the resolver now honors the resolved `local` mode and stands CI down; the confirmation stays authoritative on the local merge path, and a registered `GAIA-Audit` still blocks a button-merge behind the pending stand-down status. Pinning `local` on a ruleset-protected repo remains unsupported (use the trailer handshake there) (#532, #535)
- correct GAIA's Serena registration so Opus actually reaches for Serena's symbol tools instead of its own built-in search. Both setup surfaces now register the MCP server in the `claude-code` context with `--project-from-cwd` auto-activation and pair it with Serena's system-prompt override (the `--append-system-prompt` launch form plus an always-loaded fallback rule), replacing a desktop-app-context registration with no project activation and no override that left symbol queries almost never routed to Serena. The code-search guard now also catches a single bare-identifier symbol `grep`/`rg`/`ag` issued through `Bash`, not just the structured `Grep` tool, staying conservative so pipelines, compound commands, quoted or regex patterns, and searches outside `app`/`test` TS/TSX pass through untouched; the Serena usage scan counts shell greps too, so its search-volume denominator is honest. Adopters without Serena registered are unaffected (#531)
- close a series of distribution-boundary leaks, mostly surfaced by `/health-audit` and its false-clean challenger, where a maintainer-only path referenced a shipped or adopter-facing surface: a dangling comment in the `code-review-audit` workflow template pointed at the never-installed `forensics-triage.yml` (neutralized to a generic pinned-SHA note), descriptive docs still referenced the release-excluded `.github/forensics/` directory (a `/gaia-harden` routing example genericized; the three legitimate references kept and allowlisted), and two shipped skill docs named maintainer paths (scrub-wrapped). The bundle-time scrub's `maintainer-paths`/excluded-slug check now derives its excluded set from `.gaia/release-exclude` at scan time instead of a drifting hand-maintained regex, covers the maintainer-only CLI binary `.gaia/cli/gaia-maintainer` (no live leak found there, the sole shipped occurrence in `wiki/concepts/Telemetry.md` was already allowlisted) and `.github/forensics/`, and the leak-check greps are tightened; the manifest is regenerated fresh against the exclusion list. All maintainer-only and config-driven, so no CLI rebundle (#430, #434, #497, #499, #527, #528, #529)
- `/gaia-wiki` now lands its maintenance PR like any other merge: `gaia wiki chain finish` (and standalone `sync`'s protected-branch path) waits for the PR to merge, then pulls the base and deletes the local `wiki-sync/*` branch, instead of enabling auto-merge and returning immediately, which left local `main` un-pulled and the branch lingering until the session-start janitor swept it. If the merge does not land within the bounded wait, auto-merge stays queued and cleanup falls back to the janitor, so the command never hard-fails (#514)
- harden `/gaia-init` (and its `/setup-gaia` successor) across real init runs: one AskUserQuestion timeout no longer cascades into unattended auto-defaulting of every downstream identity gate (each gate evaluates independently, and an unknown CODEOWNERS handle ships a loud placeholder rather than a fabricated value), and init no longer git-mv's or advertises the release-excluded CI workflows that are absent from the scrubbed tarball. Real-run defects are fixed, `pnpm install` survives `--no-tty`, spec-kit registration stages in-project instead of a sandbox-forbidden `/tmp` path, and the Serena verify probe anchors correctly; and react-doctor v0.5.8's changed install layout is handled end to end (the non-Claude `.agents/skills/` copy is stripped, the verify probe repoints to `.claude/skills/react-doctor`, and `/setup-gaia`'s `pnpm pkg delete` uses the bracket-quote form) (#417, #439, #504, #509)
- the `code-review-audit` CI workflow now stamps the durable `GAIA-Audit` commit status on a clean audit whose only tree delta is an off-limits self-heal surface (`.claude/**`, `.specify/**`, `wiki/**`, or a >10-file sprawl). Such a PR previously set `refused=true`, skipped every success-stamp step, and blocked the merge despite a clean audit (recurring on every `.claude/**`-touching PR); the gating condition is dropped and the proven-clean marker check stays the sole guard (#501)
- `/update-deps` preview and override handling are correct: a snoozed group is now marked `[snoozed until DATE]` and default-skipped (with an explicit "update everything incl. snoozed" override) instead of being silently re-offered every run, and the override audit re-resolves with `pnpm dedupe` instead of `pnpm install` (which short-circuits on an overrides-only change), asserting the lockfile `overrides` block matches `pnpm-workspace.yaml` before finishing (#388, #500)
- the statusline `Run /gaia-harden` nudge no longer silently disappears during a `gh`/network outage: the refresher read only `candidate_count` and ignored the `gh_ok` boolean, so a failed window read (which exits 0 emitting count 0) was treated as a genuine all-clear and dropped the nudge for the duration of the outage. It now parses `gh_ok` and keeps the previous cached count whenever the read failed (#494)
- make hardcoded machine-specific absolute paths repo-relative, so a maintainer who forks `gaia-react/gaia` (and any adopter scaffolding from the template) is never pinned to the original author's checkout: the `.gaia/cli` storage-slug example becomes a neutral placeholder and the `.specify` runbook derives `GAIA_ROOT` from `git rev-parse --show-toplevel`. Codifies the standing policy in a new `.claude/rules/repo-relative-paths.md` (a portable `git grep "$HOME"` audit plus two documented exceptions) (#490)
- close 25 defects found in an adversarial audit of `/gaia-forensics` (the end-user bug-report skill and its maintainer-only triage CI). Adopter-facing: the redaction pass that runs before a report is filed to a public issue now catches secret and identity shapes it previously missed (fine-grained GitHub PATs, JWTs, `Bearer` tokens, Slack app tokens, connection-string credentials, and bare `/Users`/`/home`/`/root` paths), and the skill verifies the `gaia-forensics` label actually attached instead of reporting a false success. Maintainer-only and release-excluded: the triage workflow is hardened against prompt-injection and no longer strands an issue as triaged-but-unhandled (#487)
- `/gaia-plan` no longer fires a delete-permission prompt on every run: the post-planner verify step confirms the required artifacts exist and warns if planner scratch survived, instead of force-deleting `.work/`. The planner already removes its own scratch, so the `rm -rf` backstop only cost an interactive prompt each run (#460)
- `LanguageSelect` renders nothing when only one language is configured: a single configured locale gave the switcher a pointless one-item dropdown, so the guard returns `undefined` when `LANGUAGES.length <= 1` and self-activates once a second locale is added via the add-locale runbook. The IndexPage test and language-switch a11y E2E are split to keep both single-language and multi-locale projects green, and the `remove-i18n` runbook gains a step to drop the dangling `LanguageSelect` import (#457)
- the `/gaia-wiki lint` and `/gaia-wiki consolidate` playbooks `mkdir -p wiki/meta/` before their first report write: `wiki/meta/` is release-excluded, so a freshly scaffolded adopter clone had no such directory and the first run failed with `No such file or directory` (#447)
- close the numeric-0 `&&` leak in shipped `Form` components and `ErrorStack`: `Checkbox`, `Field`, and `FieldLabel` boolean-coerce their `ReactNode` `&&` operands so a falsy operand can no longer render a literal `0`, and render returns move from `return null` to `return undefined` (`MetaHydrated`, `FormError`, `ErrorStack`) (#441)
- correct two `/gaia-spec` finalize/close defects: the documented manual close handle changes from `/gaia-spec close` (an unimplemented router branch that instead started a new SPEC) to the real `/speckit-gaia-spec-close`, and `gaia telemetry emit --abandoned` becomes a valued boolean (`--abandoned true|false`) so the success-path spec-timing signal is no longer silently dropped (#422, #438)
- reconcile shipped check definitions that contradicted GAIA design decisions and drove auditor variance or false flags: the `code-review-audit` bundle-size "named imports over namespace imports" check gains the documented-barrel carve-out its react-doctor sibling already had, the Claude Integration Fitness checks stop flagging a quoted counter-example path, and the worthiness presence gate retires an inert double-gating cross-check. Page-folder naming standardizes on `{PascalName}Page` across the affected skills and decision pages (#431)
- Playwright e2e self-heals the cold dev-server hydration race: `hydration()` probes for the hydrated meta then reloads once onto the now-optimized Vite bundle, and a global-setup `/` warm-up front-loads the first dep-optimize, so a cold `pnpm pw` passes without the local retry mask (#400)
- `/gaia-audit` auto-applies a clean, zero-action audit instead of parking it at the decision gate, closing the path where a `draft` report dangled and the statusline nudged "resume draft" indefinitely; every Stage 2 apply path busts the statusline cache so the nudge clears immediately (#416)
- the `gaia scaffold` generators handle real inputs: `scaffold route` resolves output paths from `cwd`, emits a flat locale file, names page folders `<Pascal>Page`, fails loudly when the locale barrel is missing, and gains a `--dry-run` flag; and `scaffold component --props` accepts comma-bearing prop types (`Record<string, unknown>`, multi-arg function props, tuples) by splitting only on depth-0 commas (#397, #411)
- consolidate the react-doctor config to `doctor.config.ts` (matching the repo's `*.config.ts` convention), with a pre-commit hook and CI check guarding against a duplicate config silently shadowing it; and `app/root.tsx` now escapes `<` in the inlined `window.process` env JSON so an env value containing `</script>` cannot break out of the inline script (#394)
- the `code-review-audit` self-heal step resets `claude-code-action`'s untrusted-PR file restore (`.husky`, `.mcp.json`, `CLAUDE.md`, and siblings) to HEAD before staging, so it no longer emits a spurious self-heal commit that silently reverts legitimate PR changes to those paths (#395)
- clear named toolchain security advisories across the app and the maintainer CLI: bump `@babel/core` (GHSA-4x5r-pxfx-6jf8), `js-yaml` (GHSA-h67p-54hq-rp68), and `esbuild` (GHSA-g7r4-m6w7-qqqr), and pin the `.gaia/cli` `vite` devDep (GHSA-fx2h-pf6j-xcff); `js-yaml` is bundled into the `gaia` CLI binary, which was rebuilt. In the same workspace the `tsx` devDependency is bumped to clear its transitive `esbuild` off the vulnerable floor, resolving Dependabot alert #116 (#413, #453)
- the `code-review-audit` CI workflow now upserts a single PR status comment instead of posting a fresh one on every push, so a PR carries one status comment that updates in place rather than a growing stack of duplicates. The comment carries a stable HTML-comment marker so the workflow locates and edits it across synchronize events (#622)
- the `/gaia-plan` worktree-mode continuation prompt now hands the user a cleanup command that works. Its isolation-context fallback told the maintainer to run `ExitWorktree({worktree: ...})` from a fresh session, a call the tool rejects on signature and would no-op even if accepted (`ExitWorktree` only touches a worktree created in the same session), so a merged worktree was left stranded under `.claude/worktrees/`. It now emits the session-independent `git worktree remove --force` + `git branch -D` shell cleanup, matching the form `/gaia-debt` already uses (#636)
- the taxonomy's shipped-`.sh`-scope-gap Issue Class is now codified instead of relying on manual inspection: `gaia-maintainer release manifest --check` gains `lintScanScopes()`, which cross-references every owned `.sh`-bearing directory against both the scrub `maintainer-paths` scope and `runtime-deps`'s `SCAN_GLOBS`, so a shipped script tree can no longer fall outside both distribution-boundary leak checks at once with nothing to catch it. Maintainer-only tooling, so no adopter clone is affected (#664)

## [1.6.1] - 2026-06-12

### Added

- land the full chain on one branch and one PR (#379)
- prune prior-run artifacts at the start of a confirmed update (#376)
- accept a verbatim audit-workflow re-render as a no-marker bypass (#375)
- auto-render stale audit workflow on update (#371, #372) (#374)

### Changed

- shrink dep-audit.md to a wiki pointer (#380)
- pnpm-workspace.yaml must be copied into Docker pnpm stages (#373)

### Fixed

- exclude .claude-pr/ from the stamp dirty check (#370)

## [1.6.0] - 2026-06-11

### Added

- CI audit progress breadcrumbs: the `code-review-audit` workflow prints a curated per-phase timeline (scope resolved, oracles done, holistic review done, adversarial verify done, report stamped) into the GitHub Actions step summary, giving the otherwise-silent CI run a public-safe signal of progress with no raw tool output or secrets, and a breadcrumb write never blocks the audit (#350)
- `/gaia-harden`, a human-gated Policy-Memory Loop: when `code-review-audit` flags the same finding class across three or more distinct PRs within a rolling 90-day window, the statusline nudges, and running `/gaia-harden` judges the lightest durable enforcement form (a path-scoped rule, a deterministic check, or a skill) and drafts it for approval, committing nothing without your say-so (#321)
- `/update-deps` interactive preview and snooze: updates are grouped (major, minor, patch, non-semver) and you can defer any group before applying; snoozes persist and resurface after 14 days or when a newer version ships, and `CI=true` or `--scope` runs skip the preview (#347)
- a React Router local-docs rule: a path-scoped rule points framework-mode work at the version-exact docs React Router ships as markdown under `node_modules/react-router/docs`, falling back to the online docs only when the local copy is absent

### Changed

- pnpm upgraded from 10.33.0 to 11.5.2, with `packageManager` pinned to `pnpm@11.5.2`; workspace settings (`overrides`, `allowBuilds`, `publicHoistPattern`, `savePrefix`, `strictPeerDependencies`) move from `package.json` and `.npmrc` to `pnpm-workspace.yaml` (#333)
- `/update-gaia` merges `pnpm-workspace.yaml` field by field, keeping adopter-only overrides and build approvals while applying the release delta (#335)
- `/gaia-audit` researches first and presents a single Apply, Discuss, or Decline gate before changing any file, adding a CONFLICT finding class, a resumable report lifecycle with a 72-hour re-apply grace window, post-apply verification, an optional scope-hint argument, and a statusline nudge from drift/budget/draft signals; the apply stage runs on Sonnet (#326)
- `/gaia-fitness` renders its report as a deterministic width-aware ASCII card (#320)
- TypeScript 7 readiness: `tsconfig.json` adopts `stableTypeOrdering` and `noUncheckedSideEffectImports` while still on TypeScript 6 (#331)
- `code-review-audit` runs its holistic review on Opus with a coverage-first pass that surfaces every candidate (tagged with severity and confidence) before adversarial verification filters them (#353, #354)
- `/gaia-plan` and `/gaia-spec` dispatch every subagent from a single depth-1 orchestrator, with per-bucket Haiku and Sonnet model pinning (#363)
- the `/gaia-wiki` maintenance chain moves to the same single depth-1 orchestrator topology (#364)
- skeleton-loaders: static translatable text (labels, headings, button text) must use `t()` or `<Trans>`, and skeleton containers require `role="status" aria-busy="true"` (#360)
- statusline: `/update-gaia` now precedes `/update-deps`, and the harden nudge reads `Run /gaia-harden (N recurring patterns)` (#328)
- dependency refresh: react and react-dom 19.2.7, storybook 10.4.2, axe-core 4.12.0, chromatic 17.2.0, happy-dom 20.10.1, i18next 26.3.1; the `brace-expansion` and `ws` CVE overrides are dropped (resolved natively) and the `qs` override is retained (#332, #348)
- the `react-router` group is bumped to 7.17.0 (`react-router`, `react-router-dom`, and the `@react-router/*` packages), which ships its official docs as markdown under `node_modules/react-router/docs` for local lookup
- `/update-deps` and `/update-gaia` repoint dependency-override management to `pnpm-workspace.yaml` for pnpm 11 (#334)
- react-doctor reads a single `doctor.config.jsonc`; the legacy `react-doctor.config.json` is removed from the template (#327)
- the project `CLAUDE.md` response-style guidance is scoped to conversation, with a coverage carve-out for audits, reviews, plans, and specs, and a coaching register (#358)
- `coding-guidelines` clarifies that the impossible-scenario test ban does not cover real failure modes such as non-zero exits, loop non-convergence, and network errors (#359)
- CLI internals cleanup: several dead or unwired maintainer subcommands are retired, a subcommand reachability guard rejects calls to commands the binary no longer exposes, and the CI audit gains a `--verbose` log mode (#336, #337, #338, #339, #340, #341)
- `/gaia-release` (maintainer-only) wires the `release-notes` skill into the website lockstep, overwrites the GitHub release body with the generated adopter-facing notes, and adds a docs-site version lockstep step (#318, #319)
- load-bearing wiki fetches in the instruction files are marked imperative (must-read) and deep-dive pages are tagged, tightening how the assistant pulls wiki context during a task (#361)

### Fixed

- the bare-test guard no longer false-positives on quoted prose: detection anchors to command position, so a `pnpm test` phrase inside a commit message or a `--body` string is no longer blocked, and the sibling RED-observation gate is anchored the same way (#345)
- type-only tests no longer hit an unsatisfiable TDD RED-verification gate: the signal helper classifies each test runtime vs type-only and the commit check exempts type-only tests, delegating their correctness to the `tsc` quality gate (#344)
- `/update-gaia` defers the VERSION-file bump until after the run summary prints, so an interrupted run resumes at the baseline version instead of dead-ending as already up to date (#362)
- `/update-deps` and `/update-gaia` bound their retry and heal loops to one remediation pass plus a single gate re-run, then revert and log, instead of iterating open-ended (#355)
- the autonomous `/gaia-audit` and `/gaia-fitness` re-run paths require a fresh subagent on every stateful re-run, so a resumed cycle can't silently reuse a stale in-context report (#356)
- `/update-deps` override audit adds a security-floor pin test so a CVE-pinned override is never dropped as obsolete while a live advisory remains (#348)
- maintainer-only source paths no longer leak into adopter-facing surfaces and a stale manifest is regenerated; the `/update-deps` skill drops a maintainer-only path reference absent on adopter clones (#367)
- Playwright retries once on local runs to absorb the cold optimize-cache flake (#349)
- the `.specify` GAIA commands dispatch tools and skills explicitly instead of emitting prose narration (#357)
- the CLI test suite triggers on `code-review-audit.yml` edits, closing a gap where workflow changes shipped without exercising the CLI tests (#317)

## [1.5.0] - 2026-06-05

### Added

- dependency-CVE deterministic-oracle advisory (#299)
- mechanical TDD RED-verification (SPEC-003) (#297)
- decouple from upstream claude-obsidian, add native checks (#289)
- add maintainer-only release-notes skill (#283)

### Changed

- replace the `/gaia <subcommand>` router with discrete `/gaia-*` slash commands (`/gaia-plan`, `/gaia-spec`, `/gaia-audit`, `/gaia-fitness`, `/gaia-forensics`, `/gaia-wiki`, `/gaia-handoff`, `/gaia-pickup`) so every workflow surfaces in autocomplete; the old space-form `/gaia <sub>` is removed, and sub-arguments are unchanged (#277)
- add /gaia-fitness to commands table (#281)

### Fixed

- repair PR-merge audit gate (specialist dispatch + clean no-change marker) (#313)
- exclude self-referential wiki-sync commits from drift count (#311)
- strip git-quoted paths in inspectWorkingTree (#308)
- run sync on Sonnet and add fabrication guard (#307)
- stop runtime-deps flagging prose path tokens as leaks (#304)
- repair flaky wiki-sync release-gate smoke harness (#303)
- repair two pre-existing release-gate failures (#302)
- close distribution-boundary leak failing the smoke suite (#301)
- guard CI-defer source so missing lib doesn't kill wiki hooks (#298)
- anchor git guards to command position (#296)
- exempt non-blocking residuals from the clean-A+ gate (#293)
- alias-proof bare `gaia` CLI calls to repo-relative `.gaia/cli/gaia` (#291)
- regenerate report fresh each run; never reuse a stale #11 (#286)
- out-of-scope bypass for docs/metadata-only PRs (#276) (#280)
- `/update-gaia` merges `package.json` field-aware instead of as one opaque blob: it never touches adopter identity fields, applies only the real upstream dependency/script delta, and never re-adds a dependency the adopter removed, so a version-only release stops emitting a full-file conflict patch (#275) (#279)
- inline literal sibling-repo paths in gaia-release pushes
- add repo-scope.sh to audit-ci-tests paths filter
- the repo-scope guard strips surrounding quotes from `git -C "<path>"` and `cd '<path>'` captures, so a quoted sibling-repo push is recognized as foreign and allowed instead of denied as a home-repo push to main

## [1.4.0] - 2026-06-02

### Changed

- promote machine-local feedback memories into shared wiki (#270)

### Fixed

- derive wiki playbook dates from the shell clock, not the model (#269)
- /update-gaia: scope the three-way merge to the real release delta; respect adopter-deleted files instead of re-injecting them, and skip owned files the release left unchanged instead of emitting spurious conflict patches (#268)

## [1.3.5] - 2026-06-02

### Added

- install code-review-audit on demand; honor both Claude tokens; key merge decision on workflow presence (#262)
- honor pnpm 11 minimumReleaseAge in version selection (#260)
- stamp GAIA-Audit status on out-of-scope skips (#257)
- gate expensive checks on the since-last-green delta (#254)
- enforce Content-Security-Policy for scripts (#253)
- incremental code-review-audit scope (#252)
- pnpm supply-chain hardening: root `pnpm-workspace.yaml` with `minimumReleaseAge` (7-day quarantine) and `trustPolicy: no-downgrade` (#251)

### Changed

- mark CI code-review-audit as opt-in (#263)
- clarify the gate owns formatting; make PR merge marker-first (#261)
- print kickoff prompt instead of copying to clipboard (#259)
- document useDebounce return semantics (#255)
- enable React Router v8 future flags for early v8 readiness: `v8_passThroughRequests`, `v8_splitRouteModules`, `v8_trailingSlashAwareDataRequests`, `v8_viteEnvironmentApi` (#251)
  - **Migration (`v8_passThroughRequests`):** loaders/actions now receive the raw `request`, so `request.url` keeps the `.data` suffix and `?index`/`?_routes` params on data requests. If you customized `app/root.tsx` (or any loader) and call `new URL(request.url)` for normalized routing, switch to the new normalized `url` arg (a `URL` instance), e.g. `({request, url}) => url.pathname`. `/update-gaia` delivers the updated `app/root.tsx` as a conflict patch for customized files, so apply this by hand when resolving it.

### Fixed

- detect orphaned wiki drift in preflight via suggested_base (#258)
- recover the un-evaluated window on sync re-anchor (#256)
- gaia-release updates softwareVersion
- `Form/Chain` composes `className` with `twMerge` instead of `twJoin`, so a consumer's utilities override the component's defaults (#251)

## [1.3.4] - 2026-05-26

### Added

- bypass audit-marker requirement on chore(deps) PRs (#246)
- skip required workflows on chore(deps) PRs (#245)

### Changed

- reframe dispatched-check rollup ADR around polling architecture

### Fixed

- seed missing files; round-trip GH issues; drop UAT leaks (#247)
- poll dispatched runs and stamp jobs into PR rollup (#243)
- stamp dispatched check runs into PR rollup (#240)
- re-trigger required checks on self-heal HEAD (#238)

## [1.3.3] - 2026-05-22

### Added

- `gaia setup-ci check-drift`: primitive that byte-compares rendered `.github/workflows/gaia-ci-*.yml` against a fresh in-memory render. Reports `{drifted, missing, in_sync}` per tool.
- `/setup-gaia-ci` Step 2 calls `check-drift` on configured repos. On drift, prompts for re-render / skip / full reconfigure instead of unconditionally short-circuiting.
- `/setup-gaia-ci` Step 11.5: lightweight drift-fix branch + commit + PR path. Regenerates only the workflow YAML; tool selection and bot token untouched.

## [1.3.2] - 2026-05-22

### Fixed

- dedupe audit issues, scope pre-run-skip per-tool, set git identity (#233)

## [1.3.1] - 2026-05-22

### Fixed

- `create-worktree.sh`: WorktreeCreate hook that creates the worktree and returns its path (#231)
- `update-gaia`: remove phantom `gaia update merge` CLI call from Step 7 (#231)
- `update-gaia`: Step 9 cache-bust writes the new version instead of preserving stale fields (#231)
- `update-gaia`: open a PR at the end of the run instead of stranding the branch (#231)

## [1.3.0] - 2026-05-22

### Added

- allow `/gaia plan` and `/gaia spec` to run concurrently in separate sessions without clashing; collision-proof atomic writes prevent racing writers from corrupting committed state (#198, #207)
- add axe-core accessibility testing for Vitest and Playwright (#205)

### Changed

- derive controlled counter from value, document latest-ref pattern (#217)
- thread auth and language per request (#194)
- single-pass deepRemoveNil
- note why the NonceContext default is an empty string
- record the unsafe-inline and no-report-uri trade-offs
- rename `/setup-gaia` command to `/setup-cloned-gaia-project`

### Fixed

- add 15_000ms timeout to per_domain_page_counts test (#225)
- macOS sed portability + +types ignore bucket (#221)
- accept GitHub commit status as merge gate signal (#219)
- skip setLocalLength in controlled mode, move latest-ref comment (#218)
- deferred code-review-audit findings from PRs #190-214 (#216)
- a11y/perf follow-ups for Checkboxes, FieldDescription, TextArea (#213)
- a11y for field descriptions, drop nested live region (#212)
- audit small fixes: amended_rate, rollback msg, parseKeyPath, generator dup, a11y (#210)
- revert-ledger lock per-PR scope + stale recovery (#209)
- robust flag derivation in sync-land (#208)
- atomic-write deferred writers + crypto temp-path (#207)
- cover .playwright/ entry points (#206)
- correctness gaps in init/schemas/storage (#204)
- correctness gaps in release/scaffold (#203)
- correctness gaps in automation/telemetry (#202)
- correctness gaps in intent-clarity-gap (#201)
- correctness gaps in setup-ci/ci/setup (#200)
- correctness gaps in wiki/update/update-deps (#199)
- atomic file writes with fsync (#198)
- close correctness gaps in utils and api helpers (#197)
- close XSS vector and error-handling gaps (#196)
- correct edge cases across form components (#195)
- preserve falsy success values
- reject non-local redirect targets
- add GAIA-Audit commit-status fallback to the skip gate
- write GAIA-Audit commit status instead of pushing an empty marker commit
- replace client-hints reload with pre-paint inline script
- implement per-request nonce Content-Security-Policy

## [1.2.3] - 2026-05-20

### Added

- repo-scope main/PR-merge guards + create-gaia release lockstep

### Fixed

- update workflow template snapshots for id-token permission addition
- add json-strip transform to stop maintainer-only package.json keys reaching adopters
- harden block-main against multiple git -C flags
- close multi--C ambiguity in repo-scope; handle --repo= form

## [1.2.2] - 2026-05-19

### Fixed

- drop default id-token: write from pnpm-audit workflow

## [1.2.1] - 2026-05-19

### Added

- RUNNING sentinel + concurrent orchestrator detection (#180)

### Changed

- drop second inline PR ref in Release Workflow prose
- drop inline PR ref in Release Workflow prose
- correct the release PR merge step

### Fixed

- health-audit distribution-boundary remediation (#182)
- Linux WSL compatibility for husky hook and CI workflow permissions (#181)
- atomic mkdir prevents TOCTOU race on concurrent plan creation (#179)

## [1.2.0] - 2026-05-12

### Added

- `/gaia fitness`: Claude-integration health check + auto-heal (#169, #171)
- ai shimmer animation on `gaia-logo.svg` (#168)

### Fixed

- `release preflight` tolerates wiki-sync squash-artifact drift (this release)

## [1.1.1] - 2026-05-11

### Fixed

- gaia release reference to maintainer
- bug fixes from live init run (#165)

## [1.1.0] - 2026-05-10

### Added

- robust a11y tooling stack (#156)
- close slice-4 forward-refs (wiki + update-deps) (#153)
- add Phase A configure-automation step (#148)
- spec-001 slice 4: /setup-gaia-ci slash command (Phase B) (#147)
- spec-001 slice 3: GAIA CI workflow YAML generation (#146)
- spec-001 slice 2: auto-merge + auto-revert workflow shape (#145)
- spec-001 slice 1: smart-cron + per-tool state files (#144)
- add probe-after verification pass to Step 8 (#139)
- surface GAIA-Audit trailer invalidation count in summary (#140)
- rename /wiki-{sync,consolidate,lint} → /gaia wiki <sub> to resolve plugin collision (#121)
- /gaia spec auto + branch-default plan isolation
- code-review-audit CI gate + GAIA-Audit trailer skip mechanism (#117)
- autonomous triage workflow for gaia-forensics issues (#104)
- /gaia forensics: end-user bug-report bridge (#105)
- bundle-time scrub + runtime-deps primitives (#98)
- telemetry v1 (SPEC-001): three-stream architecture (#91)
- tree READMEs + inventory audit appended to SPEC-005
- rule file + port smoke-uat-write + relocate serena
- wire before_implement hook + speckit.gaia.uat-write into manifest
- wiki-sync handoff + revised-contracts §2 amendment
- implement before_implement UAT renderer + templates + slash command
- wiki-promote routing + page render + cross-links
- wiki-promote PR-merge detection + spec-close drain
- add UAT divergence policy rule stub
- manifest entry + wiki-promote command skeleton
- install spec-kit + GAIA extension + GAIA preset during scaffold
- real spec-kit extension + preset for GAIA
- salvage GAIA wrapper scaffold + utilities from PR #84
- Serena MCP server registered by `/gaia-init` for LSP-backed code intelligence (pinned, requires `uv`); a `code-search` rule routes Claude to Serena for TS/TSX symbol queries (#82)
- dead-code detection via [knip](https://knip.dev): run `pnpm knip` after refactors or before release-candidate PRs, with a template-aware config that marks GAIA's library surface as entries so intentional exports aren't flagged (#80)

### Changed

- **BREAKING:** `/wiki-sync`, `/wiki-consolidate`, `/wiki-lint` slash commands are removed. Use `/gaia wiki sync`, `/gaia wiki consolidate`, `/gaia wiki lint` instead, or `/gaia wiki` for the full chain. Motivation: `/wiki-lint` collided with the `claude-obsidian` plugin's skill of the same name. Moving everything under the `/gaia` router namespace eliminates the collision and groups wiki maintenance with the other GAIA workflows. Hooks (`wiki-drift-check`, `wiki-commit-nudge`, `wiki-session-stop`) and statusline now point at the new names. Smoke tests under `.gaia/tests/smoke/wiki-sync/` updated. The playbooks moved from `.claude/commands/wiki-{sync,consolidate,lint}.md` to `.claude/skills/gaia/references/wiki/{sync,consolidate,lint}.md`.
- `/gaia audit` no longer covers intra-wiki duplication or broken-wikilink checks; those overlapped with `/gaia wiki consolidate` and `/gaia wiki lint`. Run `/gaia wiki` separately for wiki-internal audits.
- codify post-merge state verification + --auto vs --admin (#155)
- tighten adopter README template (#138)
- pre-launch gap fixes (CI opt-in, rollback, WSL stance) (#137)
- add manual smoke procedure for before_implement hook
- scrub fictional on_save references from smoke.md

### Fixed

- exclude CLI-Binary-Split ADR; add automation.json sentinel (#163)
- replace rm cache-bust with Write tool to avoid hook conflict (#162)
- bust cache immediately on confirmed updates (#161)
- defer stamp push until after marker write (#158)
- split release CLI into maintainer-only binary (#133)
- push GAIA-Audit empty commit to upstream so CI's audit skips (#130)
- skip audit on workflow self-mod, reword abort message (#125)
- surface code-review-audit aborts as red checks (#123)
- CI source-changes gate, knip binaries, instruction-file paths (#122)
- relocate audit-stamp bats + repair latent path bug in 4 existing bats (#119)
- SPEC-005 shared-state symlinks + slash-command worktree guards (#118)
- address remaining audit suggestions from PR #114 (#116)
- audit advisory follow-ups (S-1/S-2/S-3) (#114)
- bootstrap-labels.sh: shorten needs-human desc, add gaia-forensics (#113)
- SPEC-003 triage workflow correctness + hardening (#110)
- close SPEC-004 sharp edges (setup-state, pre-merge audit, post-merge cleanup) (#109)
- address pre-merge audit findings (#106)
- enforce exclusive ceiling in spec-kit version-check

## [1.0.5] - 2026-05-04

### Added

- v1.0.5 wiki sync system: drift-check, commit-nudge, stop-safety-net hooks plus `/wiki-sync` workhorse for a convergent wiki-update model.

### Changed

- `/gaia-init` i18n setup is now language-aware: asks the user's primary language and optional additional locales, with an opt-out path that strips i18n entirely. Per-locale `add-locale` and `remove-i18n` instructions ship as parameterized runbooks under `.claude/instructions/`.
- Pin docs install command to `npx create-gaia@latest`.

### Fixed

- Restore statusline indicators for `/update-deps` and `/update-gaia`. The prior SessionStart hook approach was invisible to users; system-reminders only reach the model, and a 6h snooze locked in regardless of whether the user ever saw a prompt. Statusline indicators are passive and always visible.
- `/gaia-release` Step 2 gate now allows wiki-prefix-only drift, and `/wiki-sync` Step 7 is branch-aware (branch+PR on `main`, in-place commit elsewhere). Together they make the `/wiki-sync` → `/gaia-release` flow self-consistent.
- `/gaia audit` now chains research and apply by default; `--apply` is the retry escape hatch.
- Wiki sync system: smoke test assertions match the frozen interface.
- `/gaia-release` and `/gaia-init` scrub templates for `wiki/hot.md` (and `/gaia-release` Step 9 for `wiki/log.md`) now include the full frontmatter required by `/wiki-lint` (`status`, `created`, `tags`), eliminating a recurring lint regression on every release.

## [1.0.4] - 2026-05-01

### Fixed

- Handle `git -C <path>` in block-main-destructive-git hook

### Changed

- Wiki lint + audit hygiene sweep

## [1.0.3] - 2026-05-01

### Fixed

- Remove if conditionals from PreToolUse/PostToolUse hooks

## [1.0.2] - 2026-05-01

### Fixed

- Added `pnpm.onlyBuiltDependencies` for `core-js-pure`, `esbuild`, `msw`, and `unrs-resolver` to silence the pnpm build-script warning on fresh installs.

## [1.0.1] - 2026-05-01

### Fixed

- `/init` interceptor now reliably redirects to `/gaia-init`. The previous implementation used `UserPromptSubmit` + `exit 2`, which blocked the turn entirely so the model never ran. Switched to `UserPromptExpansion` (matcher: `init`) with `additionalContext` only; the model receives `/init`'s expansion plus a system-reminder override telling it to invoke `/gaia-init` via the Skill tool. The user-visible "blocked by hook" banner is gone.

## [1.0.0] - 2026-04-30

### Initial release

GAIA v1.0.0 is the inaugural public release of the GAIA React workflow, a Claude-native foundation that ships skills, commands, hooks, a wiki, and a curated React Router 7 app skeleton designed for agentic development from day one.

#### Highlights

- **Claude integration surface.** `.claude/` ships with rules, settings, hooks, an agent skills bundle (`typescript`, `react-code`, `tailwind`, `tdd`, `skeleton-loaders`, `playwright-cli`, `eslint-fixes`, `update-deps`, scaffolders), and an agent commands catalog. `CLAUDE.md` is curated for context economy.
- **GAIA workflows.** `/gaia plan`, `/gaia handoff`, `/gaia pickup`, `/gaia audit` cover task orchestration, session continuity, and knowledge-store hygiene. `/gaia-init` bootstraps new projects from the template.
- **Wiki vault.** Architecture overview, decisions (Quality Gate, pnpm, Dark Mode Modernization, etc.), modules (Routing, Styling, i18n, Form Components), concepts (Agentic Design, API Service Pattern, Component Testing, Task Orchestration), and a hot/log pair for session continuity.
- **App stack.** React Router 7, React 19, Tailwind v4, ESLint 9, Vite 8, Vitest 4, TypeScript 6, pnpm 10, MSW 2 + `@msw/data` 1.x.
- **Form system.** Conform + Zod for type-safe forms with reusable field components.
- **i18n.** `remix-i18next` middleware, English + Japanese language scaffolding, `LanguageSelect` component, Storybook locale switcher.
- **Dark mode.** Cookie-as-truth + `@epic-web/client-hints` + optimistic `useFetchers()` UI.
- **Quality gate.** Mandatory pre-commit pipeline: simplify, localization check, typecheck, lint, unit tests, E2E tests, dev smoke test, build. Zero warnings tolerated.
- **Release tooling.** Tag-triggered `release.yml` builds a scrubbed tarball; `create-gaia` bootstrapper consumes it via `npx create-gaia@latest my-app`.

[Unreleased]: https://github.com/gaia-react/gaia/compare/v1.6.1...HEAD
[1.6.1]: https://github.com/gaia-react/gaia/releases/tag/v1.6.1
[1.6.0]: https://github.com/gaia-react/gaia/releases/tag/v1.6.0
[1.5.0]: https://github.com/gaia-react/gaia/releases/tag/v1.5.0
[1.4.0]: https://github.com/gaia-react/gaia/releases/tag/v1.4.0
[1.3.5]: https://github.com/gaia-react/gaia/releases/tag/v1.3.5
[1.3.4]: https://github.com/gaia-react/gaia/releases/tag/v1.3.4
[1.3.3]: https://github.com/gaia-react/gaia/releases/tag/v1.3.3
[1.3.2]: https://github.com/gaia-react/gaia/releases/tag/v1.3.2
[1.3.1]: https://github.com/gaia-react/gaia/releases/tag/v1.3.1
[1.3.0]: https://github.com/gaia-react/gaia/releases/tag/v1.3.0
[1.2.3]: https://github.com/gaia-react/gaia/releases/tag/v1.2.3
[1.2.2]: https://github.com/gaia-react/gaia/releases/tag/v1.2.2
[1.2.1]: https://github.com/gaia-react/gaia/releases/tag/v1.2.1
[1.2.0]: https://github.com/gaia-react/gaia/releases/tag/v1.2.0
[1.1.1]: https://github.com/gaia-react/gaia/releases/tag/v1.1.1
[1.1.0]: https://github.com/gaia-react/gaia/releases/tag/v1.1.0
[1.0.5]: https://github.com/gaia-react/gaia/releases/tag/v1.0.5
[1.0.4]: https://github.com/gaia-react/gaia/releases/tag/v1.0.4
[1.0.3]: https://github.com/gaia-react/gaia/releases/tag/v1.0.3
[1.0.2]: https://github.com/gaia-react/gaia/releases/tag/v1.0.2
[1.0.1]: https://github.com/gaia-react/gaia/releases/tag/v1.0.1
[1.0.0]: https://github.com/gaia-react/gaia/releases/tag/v1.0.0
