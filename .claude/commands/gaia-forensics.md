---
name: gaia-forensics
description: Turn a GAIA workflow misfire into a redacted, classified, filing-ready bug report in one read-only pass. Self-diagnoses config issues inline; files probable bugs upstream on confirmation.
argument-hint: [description]
---

Run the GAIA **forensics** workflow with these arguments: `$ARGUMENTS`

Read `.claude/skills/gaia/references/forensics.md` from the project root and follow it exactly. That reference is written to consume an argument string, treat the arguments above as that input. If no arguments were provided, follow the reference's no-argument path.
