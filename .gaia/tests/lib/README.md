# SPEC-ledger lib harness

Maintainer-only bats suite for the SPEC ledger machinery:
`.specify/extensions/gaia/lib/spec-allocator.sh`, `ledger-update.sh`, and the
shared `with-ledger-lock.sh` mutex. Excluded from the release bundle via
`.gaia/release-exclude` (category `.gaia/tests/`). Every test is hermetic
each spins up its own tmp git repo via `helpers/tmp-spec-repo.sh` and tears it
down; no reliance on the real project `.gaia/local/specs/ledger.json`.

This directory also carries **doc-conformance** suites, which grep instruction
markdown rather than exercising a script. They live here because this is the
bats directory `.github/workflows/audit-ci-tests.yml` actually runs (`bats
.gaia/tests/lib/`); a doc-conformance suite landed in `.gaia/tests/sandbox/`,
which no workflow runs, would gate nothing.

## Coverage

| File                              | What it tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `with-ledger-lock.bats`           | The mutex helper in isolation (Contract C1): acquire/release, exit-code passthrough, no-stdout rule, forced mkdir fallback, acquisition timeout → 75, stale-lock recovery, flock path when present.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `spec-allocator-concurrency.bats` | Allocator + ledger-update concurrency (Contracts C2/C3): N-parallel `next` with no lost rows and no duplicate ids (real flock and forced fallback), stale-lock recovery, `in_progress` draft surfacing + legacy fallback + none, read-only modes take no lock, exit-code preservation, and the cross-script `ledger-update` racing `next` interleaving.                                                                                                                                                                                                                                                                                                      |
| `spec-folder-layout.bats`         | Per-SPEC folder layout + migration (Contract C7): `spec-folderize.sh` happy path (flat + `archived/` → `<id>/SPEC.md`, byte-identical contents), idempotent re-run, `--dry-run` no-op, empty-tree no-op, flat+foldered conflict → exit 4, tracked-vs-untracked move strategy (`git mv` vs `mv`); allocator `highest`/`next`/`in_progress` over foldered specs (`next` creates no folder, ledger-first wins, foldered fallback resolves); `spec-renumber.sh` folder rename keeping `SPEC.md` named `SPEC.md` with siblings carried + collision guard; a flat→folderize→`next`→renumber→archive round-trip; read-only modes take no lock and create no folder. |
| `doc-isolation.bats`              | Doc-conformance for the shared isolation reference (`.claude/skills/gaia/references/isolation.md`) and its two callers. Byte-identity: a bash renderer takes the prompt literals, the option order, and the lead option out of the fragment, substitutes each caller's four slots, and diffs against the golden fixtures in `fixtures/isolation/` (captured from git, never from the working tree). Plus the single `(Recommended)` append site, the single `Default. ` prefix site (pinned to the fragment, since the renderer synthesizes the prefix and would otherwise green a fragment that had dropped the rule), the single `EnterWorktree(` call, the arm ordering (forced worktree before the policy read, and it never prompts), one pointer per caller with no copied literals, the pointer-not-snapshot rule for the generated `ORCHESTRATOR.md`, and the `RESOLVED_MODE` export.                                                                                        |
| `doc-setup-gaia-isolation.bats`   | Doc-conformance for the `/setup-gaia` `## Phase 3.5` clause (`.claude/commands/setup-gaia.md`), the existing-adopter entry point for the team isolation policy. Gate ordering (the absent-config skip precedes the write shell-out; the `check-admin` probe precedes the question; the `has("isolation_policy")` + `RECONFIGURE` gate precedes the question), exactly one `AskUserQuestion` headed `Isolation policy` with neither fragment option label, no JSON-writing construct besides the one CLI shell-out, the clause's own commit, and the explainer naming all four worktree costs plus the `/gaia-plan` / `/gaia-debt` scope limit.                                                                                        |
| `doc-gaia-init-isolation.bats`    | Doc-conformance for the `/gaia-init` Step 9 isolation-policy question (`.claude/commands/gaia-init.md`), the first-adopter entry point. The `AskUserQuestion` block and the conditional `--isolation-policy` flag passed to `gaia init configure-automation`, omitted entirely on a non-response so the `/setup-gaia` Phase 3.5 clause stays reachable for every new project.                                                                                        |
| `doc-debt-query.bats`             | Doc-conformance for `/gaia-debt`'s backlog-ordering `--jq` query (`.claude/skills/gaia/references/debt.md`). Structural greps over the extracted fenced block (`--limit 1000`, the `labels` name-string projection, the dedup-key capture, the handler class resolving out of the `labels` projection rather than a superseded body-line capture, unconditional `body` with nothing nulling it, unchanged `sort_by`, plus a negative check for the original bare-`sev:` defect) plus an executable render: the `--jq` program text is extracted from the artifact with awk, never hand-copied, and run against a synthetic four-issue fixture, asserting one key at a time on the emitted `labels`, `key.line`, `handler`, and `body`, which is what measures the artifact rather than itself.                                                                                        |
| `doc-debt-closes-carrier.bats`    | Doc-conformance for the single-carrier rule in `/gaia-debt`'s playbook (`.claude/skills/gaia/references/debt.md`): the PR body is the only place a `Closes #N` may live. Section-scoped greps, one section per invariant, so a phrase surviving elsewhere in the file cannot green a section that dropped it. Pins the commit step's ban on closing keywords together with the reason it exists (a squash merge concatenates the branch's commit bodies into the merge message GitHub parses), the drop path's commit-message rewrite for a member dropped after its commits are written, the post-merge check that compares the issues the merge actually closed against the intended close set, and the load-bearing pre-implementation position of all three peels (the security, staleness, and spec screens), whose peels owe no rewrite only because they run before any commit exists. |
| `doc-difficulty-prose.bats`       | Doc-conformance for the difficulty-grading prose across the vocabulary file (`.claude/skills/file-tech-debt/SKILL.md`'s rubric, label loop, and both filing forms), both producer surfaces (`.claude/agents/code-audit-frontend.md`'s `### E. Non-security disposition pipeline`, `.claude/skills/gaia/references/audit.md`'s out-of-scope filing block), and the two consumer subcommands (`debt.md`'s `list`/`why`) that surface the grade. Section-scoped extraction terminates on the next same-or-shallower heading, not a bare `^## `, so an H3 section's H3 siblings are never swallowed into the match. Forbids a second copy of the rubric outside the vocabulary file, a concrete model name on any difficulty-bearing line, and the requirement attaching to a Code Audit Team member with no out-of-scope filing route or to `code-audit-frontend.md`'s Cross-remit findings section. |
| `doc-audit-remedy-set.bats`       | Doc-conformance for `/gaia-audit`'s over-budget remedy contract in `.claude/skills/gaia/references/audit.md` Step 3: the budget is per file with no aggregate over `.claude/rules/*.md`, the three remedies are evaluated independently rather than offered as a pick-one menu, each names its action shape, the first is one `promote` carrying `source_action: replace` and is forbidden from pairing that promote with a `shrink` on its own source, the split routes to `## Out-of-scope findings` because no action type creates a non-wiki file, and the report template's over-budget line carries the per-remedy outcome. Forbids the original menu sentence, both sentences of the superseded reading that declared the first remedy blocked, and any issue-number citation, which would mean nothing on an adopter clone. Pairs with `doc-audit-promote-source.bats`, which pins the schema and apply-loop half of the same contract; the two meet at `source_action: replace` and neither restates the other. |
| `doc-audit-promote-source.bats`   | Doc-conformance for the `promote` action's source-side contract, across `.claude/skills/gaia/references/audit.md` (the schema, the `## Actions` preamble, and the per-action loop) plus the `wiki/concepts/GAIA Audit.md` sentence describing what Stage 1 emits: the schema offers `source_action` in its three modes (`delete`, `replace`, `keep`) with the `source_before` / `source_after` pair scoped to the one mode, the apply loop's two generic drift bullets carry the field-to-file pairings for every action type, so the promote's differently-spelled fields are reached by name rather than by a promote-specific bullet; the promote-only check that remains is schema validity of the mode plus the presence of the `replace` pair, which fails closed rather than guessing and runs pre-write because a promote's mid-apply failure lands after three writes to files other than its target; the apply arm acts on each of the three modes and edits the source **last** so a failed wiki write leaves it intact; post-apply verification covers the source side and verifies `replace` positively only, since `source_before` can legitimately survive elsewhere. Whole-file absence check on `delete_source_after`, because a half-applied rename leaves Stage 2 with no arm for what Stage 1 wrote. The negative half pins what the design bought: `## Ordering` keeps one flat type order with no same-path carve-out, and the `depends_on` gate stays `delete-entry`-only, so reintroducing the promote/shrink pair behind a gated shrink fails loudly. |
| `doc-audit-verification-gate.bats` | Doc-conformance for `/gaia-audit`'s classification-verification Run-vs-Skip gate, across all three surfaces that state the criterion: the rule bullets and the Skip option's `description` in `.claude/skills/gaia/references/audit.md`, plus the `wiki/concepts/GAIA Audit.md` paragraph describing the round. Pins that Skip eligibility excludes CONFLICT-driven actions and is exclusive (so the two bullets partition with neither an overlap nor a gap), that the gate names how to establish the exclusion from the report's own `Conflicts:` count rather than by reading each `reason` (without which the failure is fail-open, since a CONFLICT-driven action is emitted as an ordinary `shrink` block and a project-internal one need not name a contradiction), and that the `(Recommended)` tag is stated once as a rule rather than baked into an option's label or description, either of which would be correct for only one branch of a dynamic recommendation. Forbids the superseded phrasing on each surface. The `shrink` / `replace` alias is the root cause it guards: they name one action type, so an unqualified "git-reversible shrinks" readmits the destructive case the Run bullet exists to force. |
| `doc-merge-workflow-fences.bats`  | Executable-truth coverage for the shell fences in `wiki/concepts/PR Merge Workflow.md`, the page `.claude/rules/pr-merge.md` makes a precondition of every `gh pr merge`. Unlike the doc-conformance suites above, which grep prose for phrases, this one **runs** the page: each test pulls its fence body out of the page at run time and executes it, so an edit to the page is an edit to what the test drives, and a transcription into the suite (which would make it one more prose-to-prose check) is impossible by construction. Three lenses. The fence set is derived from the page and reconciled against a disposition table, so a fence added with no row stops the suite rather than being silently uncovered, and a row claiming a runnable fence with no runner test is a hole that fails. Every fence, runnable or not, parses after its `<placeholder>` slots are normalized, cites only repo paths that exist, and hands each repo script only flags that script accepts. The runnable fences are then executed against fixtures and asserted against the page's own stated outcome: the per-author mode resolver leaves a `resolved_mode` and a `should_run` in scope, the spawn oracle exits 0 with deduped sorted member names that each have an agent definition, the no-op classifier reads a marker without its sidecar as a lost report and with it as real, the wave stamp lands outside the worktree-shared audit directory, the debt-origin block resolves a provenance line three-dot and unguarded and keeps running on an unresolvable base, and the disposition sidecar path lands under the main checkout. The fences reaching github.com or mutating the checkout get the static lenses only, each with its reason recorded in the table. |
| `doc-noop-terminal-contract.bats` | Doc-conformance for the no-op guard's TERMINAL action, the step taken after a second consecutive no-op, which is restated across several prose surfaces with no machine-readable artifact holding them in step. The roster half derives the surfaces stating an ending FROM the tree (by the terminal-statement phrasing itself, which is what a surface has to write to state an ending at all) and reconciles that against the expected roster in both directions, so a new surface with a third ending stops the suite rather than going silently uncovered, and a surface that drops the statement stops it too. Assertions read the terminal SENTENCE, not the whole line, because these surfaces write a paragraph per line and every such line also names dispositions and breadcrumbs carrying the word `inline`; a whole-line read greens a sentence whose ending was rewritten to something else. Every surface but the pre-merge audit gate must end inline and must not take the gate's stop-and-surface ending; the gate must not fall back inline. The declaration half pins that `wiki/concepts/Code Review Audit Agent.md` states the general ending once and owns it, admits the gate's exception together with its reason (a member's clearance marker is that member's own attestation), and no longer asserts a uniformity it does not have, and that `wiki/concepts/PR Merge Workflow.md` declares its own departure and names the owner. Plus the pointer half: `.claude/rules/subagent-dispatch.md`'s two destinations are extracted from the pointer's own text, anchored to their phrases, and each is asserted to exist and to carry the heading or the prefix text it is cited for, and no markdown path the rule cites has gone missing. Each assertion was proven against a deliberate mutation: a further surface appearing, a surface adopting the gate's ending, a surface inventing a third ending, the gate falling back inline, each declaration phrase removed, the uniformity claim restored, a pointer repointed at a missing file, at a heading its target lacks, and at a file lacking the prefix. Honest limit: this pins what the surfaces say, never what an orchestrator does when a member no-ops twice. |
| `doc-issue-claim-parity.bats`     | Doc-conformance for the `in-progress` claim's RELEASE contract, stated at two altitudes with nothing holding them in step: the always-loaded operational rule `.claude/rules/issue-claim.md` and the shipped concept page `wiki/concepts/Issue Claim.md`. Both enumerate the hand-off paths on which release does NOT happen automatically, and a path added to the rule alone leaves the page asserting a shorter list, which is the silent direction, since the merge lands, the issue closes, and only the label stays. Parity is asserted per element through a disposition table pairing the two surfaces' leads, reconciled against each surface in both directions, so a bullet with no row and a row with no bullet each stop the suite; a count-only check would be satisfied by a page carrying the right number of the wrong entries. The leads are deliberately worded differently on the two surfaces, operational against concept-level, so the table pairs them rather than demanding byte-identity, which would force the page down to the rule's altitude and recreate the duplication the page exists to avoid. Both derivations are section-scoped and anchored to the start of the line, so bold inside running prose is not collected as an entry and a renamed section fails by name instead of deriving zero. Each surface's spelled cardinality is pinned to its own derived count, which is what stops that number rotting and is why it is allowed to stay in prose at all. Plus the page's release claim carrying the three qualifiers the hook actually enforces, with the superseded unconditional spelling forbidden. That claim is extracted down to its first two sentences and the qualifiers are asserted as the phrases carrying them, never as bare words against the file: `first` is guaranteed present page-wide by the fourth parity row, which spells `the first command in its tool call` verbatim, and survives a section-scoped read too, since the paragraph's own closing rationale says `while the first is still mid-review`; `MERGED` is likewise carried by the `--auto` entry. A wider read greens a release sentence rewritten to drop both, which is the condition-B shape the suite's header rejects, turned on the suite itself. One code-coupled assertion reads the hook through a comment-stripped view and pins the no-reference lookup the branch-deleting hand-off path describes, `--json` immediately after `pr view` being the property, since gh takes the reference positionally; a superset of `--json` fields is the same arm and stays green, while the arm being deleted, surviving only as a comment, or losing its reference-carrying twin each red. Each assertion was proven against a deliberate mutation in both directions. Honest limit: everything but the last test pins what the two surfaces SAY, and the last pins one structural property of the hook, never its behaviour on any path; `.gaia/tests/hooks/issue-claim-release.bats` drives the hook itself and this suite does not restate it. |
| `run-bats-parallel.bats`         | The hand-run parallel bats runner (`.gaia/tests/run-bats-parallel.sh`), driven through its `--table` seam with injected suite tables. Its `builtin_table()` asks `.gaia/tests/bats-shards.sh` for the shard ids and emits one row per id, so a hand run and the CI matrix consume one partition rather than two descriptions of it. Exit propagation: a failing suite in the middle, the first and the last position each fails the run and is named in the `::error::` summary, which is what a bare `wait` (only the last job's status) would green. Replay determinism: sleeps invert table order against completion order and the replay still follows the table. Full capture: a stderr-only suite's bytes land in its own log and inside its replay group. Fail-closed preconditions: an empty table, a blank-lines-only table, a two-field row, an empty command field, and a log missing at replay time are each a precondition error rather than a green run over zero work. Plus the built-in table's shape and its derivation (one three-field row per shard id, each row's command naming that same shard, each label carrying the shard's own id, the row set and the row order both equal to what the sharder declares, and no quoting or globbing in any command field, pinning the no-`eval` word-splitting precondition), and the fail-closed pair behind that derivation: a sharder the runner cannot reach, and one that dies after printing plausible ids, each yield zero rows and exit 2 rather than a partial table reporting a green over less work than it claims. Plus the unique-per-invocation default log directory. Nothing here forks bats, so the suite adds no recursion and no meaningful contention when the live runner forks the shard it lives in. |
| `bats-shards.bats`               | Guards the shard assignment `.gaia/tests/bats-shards.sh` computes: the discovered `.bats` set partitions cleanly across shards, every file lands in exactly one shard, no shard resolves empty, and no file is duplicated across shards. Drives the sharder through its environment-variable directory seam pointed at doctored scratch trees rather than the real suite directories. Compares the shard union against `git ls-files '*.bats'`, the repository's own answer to which suites exist, so a suite landing outside the seam directories becomes visible to the guard rather than silently unrun, and an allowlisted non-shard runner must still exist to keep excusing the suite it names. Adversarial fixtures prove a dropped file, a duplicated file, and an empty shard are each caught rather than passing vacuously. |
| `install-bats.bats`              | Guards the vendored bats archive `.gaia/tests/install-bats.sh` installs from: an archive exists for the pinned `BATS_VERSION`, its sha256 equals the script's `BATS_SHA256`, and the two properties the pin exists for hold, the digest is checked before the extraction and nothing is piped into `tar`. Plus the assertion that rots silently rather than loudly: no executable line reaches a fetcher, so restoring the download that put GitHub's codeload on the critical path of every matrix leg reds here instead of passing unnoticed. Plus the half of a bump that leaves no other trace: the vendor directory holds the pinned archive and nothing else, so a superseded blob left behind is caught rather than carried forward. Every assertion reads the script through one comment-stripped view, which is what keeps the header's own re-vendor recipe from either tripping the absence scan or satisfying the ordering one; the recipe quotes the command lines verbatim, so both are reachable. Each assertion was proven against a deliberate mutation: a re-added fetch (`curl` and the `npx` idiom alike), a pipe into `tar`, a corrupted blob, a version bump with no matching archive, a superseded archive left in place, and an extraction hoisted above the digest check while a header comment quoted the check it jumped. |
| `audit-ci-shards.bats`           | Guards the sharded `.github/workflows/audit-ci-tests.yml`: the aggregator job keeps the workflow's declared-required context name, the aggregator reds rather than skips when any shard fails, every filter-gated step admits `workflow_dispatch`, every job carries an integer `timeout-minutes` cap that fits the self-heal poller's 2-hop ceiling, the job's matrix agrees with the sharder's own shard list, exactly one `paths-filter` step exists in the whole workflow, no `run:` body interpolates `${{ matrix.shard }}` directly, and the sandbox leg's reduced install package set stays true. |

The N-parallel test uses a start-flag barrier so the `next` calls genuinely
overlap. A passing run with no contention proves nothing; with the barrier,
the unlocked read-modify-write would fail (duplicate id + lost ledger row).
Several `@test`s assert that a non-zero exit code propagates _through_
`with_ledger_lock` (forced jq failure → 4, invalid patch → 5, lock timeout →
4); a single happy-path run stays green even with a swallowed-code bug, so
these negative assertions are load-bearing.

## Running

```bash
bash .gaia/tests/lib/run-all.sh
```

Individual test files:

```bash
bats .gaia/tests/lib/with-ledger-lock.bats
bats .gaia/tests/lib/spec-allocator-concurrency.bats
```

## Prerequisites

- `bats-core` on `$PATH`. Install via:
  - macOS: `brew install bats-core`
  - Debian/Ubuntu CI: `apt-get install -y bats`
  - Any platform: `npx -y bats@latest` (the `run-all.sh` entrypoint falls
    back to this)
- `jq` on `$PATH`
- `git` on `$PATH`

The npm package is `bats`, not `bats-core`. The project is bats-core and the
Homebrew formula is bats-core, but no `bats-core` package is published to npm,
so that spelling resolves to an E404 rather than to a fallback.

`flock` is optional. On a box without it (e.g. stock macOS) the
mkdir-fallback is the load-bearing lock path and is exercised by the forced
fallback tests; the flock-path test `skip`s with a clear reason.

## CI integration

CI should add a `bash .gaia/tests/lib/run-all.sh` step parallel to the
forensics suite, in whichever workflow runs the other bats harnesses. The
actual CI YAML edit is out of scope for this suite.
