import type {Meta, StoryFn} from '@storybook/react-vite';
import {{Name}} from '..';

const meta: Meta = {
  component: {{Name}},
  parameters: {
    controls: {hideNoControlsWarning: true},
    wrap: 'w-fit p-4',
  },
  title: '{{storyTitle}}',
};

export default meta;

{{storyDefault}}
