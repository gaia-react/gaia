# Paths excluded from the distribution tarball produced by release.yml.
# These exist in the template repo for the GAIA team's own workflow but have
# no business on an adopter's machine. Format is tar --exclude-from: one
# literal path per line, comments with `#` are honored.
#
# Anything excluded here is ALSO skipped by `gaia-maintainer release manifest`, so the
# adopter-facing manifest never references files an adopter cannot have.
# Authoritative narrative of what's maintainer-only and why lives in
# `wiki/concepts/Release Workflow.md` (Distribution Boundary section).
#
# Categories below are ordered the same way that page lists them.

# --- 1. Maintainer-only Claude surface ---
# /gaia-release cuts releases of the GAIA template itself; adopters never
# release GAIA. The release-notes skill translates GAIA's CHANGELOG into
# public release notes for the marketing site, a release-time maintainer
# task, so it ships nowhere just like /gaia-release. /health-audit is the
# maintainer-only autonomous audit + auto-heal loop (runbook lives under
# .gaia/cli/health/, already excluded wholesale via category 10). Other
# /gaia-* commands (plan, handoff, pickup, audit) are adopter-useful and
# must NOT be added here. preferred-base.sh is the maintainer's personal
# statusline left-side renderer; adopters get the right-side-only
# gaia-statusline.sh wrapper which delegates left-side rendering to the
# user's own global statusline. The three code-audit-maintainer-* agents are
# the Code Audit Team's maintainer-only members (framework shell + framework
# TypeScript + skills-prose lenses); adopters audit only their own app via
# code-audit-frontend. /distribution-audit is the maintainer's
# ship-or-withhold audit over newly-shipping files; it drives the
# maintainer-only release CLI and has no meaning on an adopter clone.
.claude/commands/gaia-release.md
.claude/commands/health-audit.md
.claude/skills/release-notes
.gaia/statusline/preferred-base.sh
.claude/agents/code-audit-maintainer-shell.md
.claude/agents/code-audit-maintainer-node.md
.claude/commands/distribution-audit.md
# The re-spawn measurement instrument is maintainer-only: the shared ledger
# lib it writes through, the session-start prune that bounds the ledger, and
# the attribution query that reads it. An adopter clone never runs it, so it
# never has a ledger for any of the three to act on.
.gaia/scripts/audit-respawn-lib.sh
.gaia/scripts/audit-respawn-prune.sh
.gaia/scripts/audit-respawn-report.sh
# verify-required-checks.sh declares the required_status_checks the GAIA team's own
# branch ruleset must enforce (job names like "Audit CI Tests" that only exist
# on maintainer-only, release-excluded workflows); an adopter clone has no such
# ruleset and none of those jobs, so the script has no adopter surface.
.gaia/scripts/verify-required-checks.sh
# Maintainer-only distribution pre-flight; drives the release-excluded gaia-maintainer binary and would mislead adopter-side agents about a manifest their clone does not have
.claude/hooks/distribution-preflight-check.sh
# The four worktree-program build checks assert invariants that are true of
# GAIA's own source and of nothing else: that each language has exactly one
# main-root resolver (Check A) and no hand-rolled re-derivation of it (Check
# B), that .gaia/state-registry.json covers every state directory, and that no
# shipped source hardcodes a path the registry already answers. An adopter does
# not write GAIA's resolvers or its registry, and every runner of these four is
# already maintainer-only (the .gaia/scripts/tests/ bats suites and
# audit-ci-tests.yml, both release-excluded), so today they ship a check no
# adopter can trigger and could not act on. Withholding them is also what stops
# their scan lists and their sibling-suite citations naming .gaia/cli/src/,
# .gaia/cli/gaia-maintainer and .gaia/scripts/tests/ inside an adopter bundle.
.gaia/scripts/check-main-root-derivation.sh
.gaia/scripts/check-registry-completeness.sh
.gaia/scripts/check-registry-source-literals.sh
.gaia/scripts/check-resolver-singleton.sh
# This check asserts an invariant true of GAIA's own source and of nothing
# else: that GAIA's audit machinery holds one base-provenance resolver and
# that GAIA's named consumers call it. An adopter does not write GAIA's
# resolvers or its consumers. Its only runners (the .gaia/scripts/tests/
# bats suite and audit-ci-tests.yml) are already release-excluded, so
# shipping it would ship a check nothing on an adopter clone invokes.
.gaia/scripts/check-base-provenance-adoption.sh
# This check asserts an invariant true of GAIA's own source: that the shared
# verb-arming decision stays a singleton and that GAIA's own hooks all adopt
# it. An adopter does not write GAIA's hooks or its shared library. Its
# only runner (the .gaia/scripts/tests/ bats suite, run from
# audit-ci-tests.yml) is already release-excluded, so shipping it would ship
# a check nothing on an adopter clone invokes.
.gaia/scripts/check-verb-arming-adoption.sh
# Task 7.3's check asserts this repository still qualifies for git's merge refusal on the tracked wiki state file; its only runner (audit-ci-tests.yml and its bats suite) is already maintainer-only, and an adopter cannot act on the failure.
.gaia/scripts/check-wiki-state-collision.sh
# The git path-quoting lint guards GAIA's own framework shell and its CI
# workflow YAML against an unquoted `diff --name-only` or `ls-files`, either of
# which lets git C-quote a path the caller then parses. Withheld on the reasoning
# .gaia/tests/shell-lint.sh gives for itself -- adopters run GAIA's bash but
# never author it, so a linter over the framework's own shell has no adopter
# surface -- and shell-lint.sh, its only runner, is already release-excluded
# wholesale via the .gaia/tests entry, so shipping it would ship a check nothing
# on an adopter clone invokes. It also sources guard-awk-lib.sh, withheld
# below, so the absent dependency would decide the withhold on its own.
.gaia/scripts/lint-git-path-quoting.sh
# The workflow run-interpolation lint guards GAIA's own CI workflow YAML, its
# composite actions, and the adopter workflow templates it renders CI from,
# against a `${{ }}` expression substituted into a `run:` script body. Withheld
# on the same reasoning as the diff-quoting lint above: shell-lint.sh, its only
# runner, is release-excluded wholesale via the `.gaia/tests` entry, so shipping
# this file would ship a check nothing on an adopter clone invokes. It also
# sources guard-awk-lib.sh, withheld below, so the absent dependency would
# decide the withhold on its own.
#
# Note what is deliberately NOT claimed here, because it is false and would be
# the premise a future maintainer re-litigates this decision from: an adopter
# clone does have a populated scan surface. This file excludes 11 of the 13
# tracked workflows individually rather than excluding the directory, so
# `tests.yml` and `chromatic.yml` ship, and `bundle:adopter` renders further
# workflows into an adopter's `.github/workflows/` from templates the manifest
# ships. The withhold rests on the absent runner, not on an empty surface.
.gaia/scripts/lint-workflow-run-interpolation.sh
# The grep ERE-escape lint guards GAIA's own framework shell, its CI workflow and
# composite-action YAML, and the adopter workflow templates against a
# backslash-escaped letter in a `grep -E` pattern, which BSD grep and GNU grep
# read differently. Withheld on the same reasoning as the two lints above:
# shell-lint.sh, its only runner, is release-excluded wholesale via the
# `.gaia/tests` entry, so shipping this file would ship a check nothing on an
# adopter clone invokes. It also sources guard-awk-lib.sh, withheld below, so
# the absent dependency would decide the withhold on its own. An adopter clone
# does have a populated scan surface, for the reason the run-interpolation note
# above spells out, so neither withhold rests on an empty one.
.gaia/scripts/lint-grep-ere-escapes.sh
# The errexit status-read lint guards GAIA's own framework shell, its CI workflow
# and composite-action YAML, and the adopter workflow templates against a `$?`
# read that `set -e` has already made unreachable. Withheld on the same reasoning
# as the three lints above: shell-lint.sh, its only runner, is release-excluded
# wholesale via the `.gaia/tests` entry, so shipping this file would ship a check
# nothing on an adopter clone invokes. It also sources guard-awk-lib.sh, withheld
# below, so the absent dependency would decide the withhold on its own. An
# adopter clone does have a populated scan surface, and the shipped workflow
# templates render into their CI carrying the class's own exposure, so neither
# withhold rests on an empty one.
.gaia/scripts/lint-errexit-status-read.sh
# The shipped-issue-reference lint enforces GAIA's own authoring obligation: a
# `#NNN` on a file GAIA ships must name gaia-react/gaia so an adopter reading it
# is not pointed at their own tracker. Withheld because that obligation is
# GAIA's, not the adopter's -- on an adopter clone a `#NNN` in a file they edited
# means their tracker, and this gate would demand they mislabel it as GAIA's. Its
# only runner, its bats suite, is release-excluded, so shipping it would ship a
# check nothing on an adopter clone invokes and whose verdict would be wrong
# there anyway.
.gaia/scripts/lint-shipped-issue-refs.sh
# The retired-label-spelling lint fails when a spelling .gaia/labels.json records in `renamedFrom` still occurs in tracked source, which is what makes a label rename complete rather than merely intended. Withheld on the same reasoning as the lints above, and on that reasoning ALONE: its only runner, the sibling bats suite .gaia/scripts/tests/lint-retired-label-spellings.bats, is release-excluded by the separate .gaia/scripts/tests entry beside it, so shipping this file would ship a check nothing on an adopter clone invokes. An adopter clone does carry both subjects, since .gaia/labels.json ships and the scan surface is the whole tree, so the withhold rests on the absent runner rather than on an empty surface.
.gaia/scripts/lint-retired-label-spellings.sh
# The errexit source guard lints GAIA's own framework shell for a source that runs with errexit armed and is not bracketed against an unparseable target. Withheld on the same reasoning as the four lints above, and on that reasoning ALONE: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-errexit-source-guard.bats, excluded by the separate .gaia/scripts/tests entry beside it. The bats suite is named explicitly because it is the BLOCKING runner, the one whose absence decides that an adopter clone gates on nothing here, and it is excluded by a different entry than the other one. An adopter clone does have a populated scan surface (it carries .claude/hooks and .gaia/scripts), so the withhold rests on the absent runner rather than on an empty surface.
.gaia/scripts/lint-errexit-source-guard.sh
# The stale-cardinal lint flags a definite cardinal in a comment or a bats test name that states how many of something the tree holds, where nothing recounts the set. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-stale-cardinals.bats, excluded by the separate .gaia/scripts/tests entry beside it. It also sources guard-awk-lib.sh, withheld just below, so the absent dependency would decide the withhold on its own. An adopter clone does carry a populated scan surface, so neither withhold rests on an empty one.
.gaia/scripts/lint-stale-cardinals.sh
# The guard-rule shell-coverage guard fails when a tracked shell file is reached by neither .claude/rules/guards-must-fail.md, .claude/rules/partial-cause-reporting.md, nor the maintainer-only pointer that carries both to the release-excluded surfaces. Withheld on two independent grounds, either of which decides it. First the absent DEPENDENCY: the pointer rule it reads lives under .claude/rules/maintainers, excluded wholesale in category 3, so on an adopter clone the file is simply gone and the check would exit 2 on every run rather than answer; shipping it would also put that path inside an adopter bundle, which the shipped-surface leak check forbids. Second the absent runner, the same one every lint above cites: shell-lint.sh via the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-guard-rule-shell-coverage.bats via the separate .gaia/scripts/tests entry. An adopter clone does carry a populated scan surface, so neither withhold rests on an empty one.
.gaia/scripts/lint-guard-rule-shell-coverage.sh
# The collapsed signal-trap lint flags a `trap` arm that binds EXIT together with INT or TERM, the shape that leaves bash resuming at the point of interruption and so makes a script uninterruptible. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-collapsed-signal-trap.bats, excluded by the separate .gaia/scripts/tests entry beside it. It also sources guard-awk-lib.sh, withheld just below, so the absent dependency would decide the withhold on its own. An adopter clone does carry a populated scan surface, so neither withhold rests on an empty one.
.gaia/scripts/lint-collapsed-signal-trap.sh
# The bundled-hooks inventory guard fails when a hook is absent from wiki/concepts/Claude Hooks.md, where a hook is anything registered in .claude/settings.json or present as an .sh at the root of .claude/hooks/, the two sources unioned. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-hook-wiki-inventory.bats via the separate .gaia/scripts/tests entry beside it. An adopter clone does carry both subjects -- it has a settings.json and it receives the page -- so the withhold rests on the absent runner rather than on an empty surface. There is a second ground here the shell lints do not have, and it is worth writing down because it survives even if a runner were later shipped: the obligation this gate enforces is GAIA's own authoring obligation to keep GAIA's page honest about GAIA's hooks. An adopter who adds a hook of their own owes no entry in a page they did not write, and this gate would red their build until they added one. The union makes that ground stronger rather than weaker: an adopter dropping a script into .claude/hooks/ trips it without registering anything at all.
.gaia/scripts/lint-hook-wiki-inventory.sh
# The wiki cached-version guard fails when a page under wiki/ carries a `version:` frontmatter field, a hand-kept copy of a number package.json already holds that nothing invalidates. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-wiki-cached-version.bats via the separate .gaia/scripts/tests entry beside it. An adopter clone does carry a wiki, so the withhold rests on the absent runner rather than on an empty surface. There is a second ground the shell lints do not have, and it is the one the bundled-hooks guard beside it also carries: the obligation is GAIA's own authoring convention for GAIA's pages. An adopter is free to cache a version on a page they wrote, and this gate would red their build for it.
.gaia/scripts/lint-wiki-cached-version.sh
# The hook advisory-classification guard fails when a hook that stops a tool call is filed under an Advisory heading on a wiki page. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-hook-advisory-classification.bats via the separate .gaia/scripts/tests entry beside it. An adopter clone does carry all three subjects -- a settings.json, the hook bodies, and the pages -- so the withhold rests on the absent runner. It carries the same second ground its sibling above does: the obligation is GAIA's own to keep GAIA's pages honest about GAIA's hooks, and an adopter classifying a hook of their own owes nothing to a page they did not write.
.gaia/scripts/lint-hook-advisory-classification.sh
# The SIGPIPE-reader lint flags a short-circuiting reader -- a `grep` or `rg` told to stop early, in whichever spelling the gate's own header enumerates -- standing downstream of a pipe where `pipefail` is armed, in a tracked shell script or in an Actions `run:` body, the shape whose pipeline status inverts on a match because the reader closes the pipe and the upstream dies of SIGPIPE. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-sigpipe-readers.bats via the separate .gaia/scripts/tests entry beside it. It also sources guard-awk-lib.sh, withheld just below, so the absent dependency would decide the withhold on its own. An adopter clone does carry a populated scan surface, so neither withhold rests on an empty one.
.gaia/scripts/lint-sigpipe-readers.sh
# The CLI workspace-floor check reports a security floor declared in a second pnpm workspace root's pnpm-workspace.yaml that the lockfile beside it does not apply. Withheld on the absent SUBJECT, which is stronger than the absent-runner reasoning the lints above rest on: the root it checks is .gaia/cli, and every file constituting that root -- src, package.json, pnpm-lock.yaml, pnpm-workspace.yaml -- is release-excluded in the category below, so an adopter clone has no second workspace root at all and the check would exit 2 on every run rather than answer. Every runner of this check is itself release-excluded too, so the withhold holds on either ground alone. The runner set is deliberately not enumerated here: `git grep -l check-cli-workspace-floors` lists every file that references the check, which is the set the runners are read out of, and nothing holds a hand-kept list to the tree, which is how the enumeration that stood here went stale. Nothing about this reaches an adopter's own dependency hygiene: their single root is audited by the shipped `/update-deps` skill and the CI audit template exactly as before.
.gaia/scripts/check-cli-workspace-floors.sh
# The awk-source library the bats-arming shell guards source; every one of those guards is itself withheld above, so nothing on an adopter clone sources it.
.gaia/scripts/guard-awk-lib.sh
# The GAIA_AWK interpreter resolver, sourced by guard-awk-lib.sh just above. Withheld on the absent consumer, a stronger ground than the absent-runner reasoning most of the lints above rest on: every file that sources this library, through guard-awk-lib.sh, is itself withheld, so an adopter clone has nothing that would ever source it. What is NOT claimed: the resolver is generic over any awk call site in the tree, not only the surface it currently serves, so shipping it later to widen GAIA_AWK's governed surface would be a re-decision rather than a correction. The consumer set is not counted here, for the reason the check-cli-workspace-floors entry above gives about its own runner set: `git grep -l guard-awk-lib` reads it out of the tree, and a hand-kept cardinal beside it is a second copy that nothing holds to the tree.
.gaia/scripts/awk-interp-lib.sh
# The awk interpreter pin flags a bare `awk`, `gawk`, `mawk` or `nawk` in command position inside the guard-awk-lib.sh closure, where the resolved GAIA_AWK is the required form, so the guards in that closure cannot run under one awk implementation locally and another in CI. Withheld on the absent SUBJECT first, which is stronger than the absent-runner ground most of the lints above rest on: its whole subject is the resolver directly above and the closure around it, every file of which is withheld, so on an adopter clone this gate would scan a surface that does not exist and refuse at its empty-closure status on every run rather than answer. Every runner is withheld too, so the decision holds on either ground alone: shell-lint.sh via the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-awk-interpreter-pin.bats via the separate .gaia/scripts/tests entry beside it. What is NOT claimed: the detector is a generic command-position reader and would work over any surface, so shipping it later to pin an adopter's own awk calls would be a re-decision rather than a correction. Nor is the wider tree claimed even here -- the gate governs the closure alone, and the command-position awk sites elsewhere in this repository are a stated non-claim in its own header, not an omission this entry repairs.
.gaia/scripts/lint-awk-interpreter-pin.sh
# The tracked-path discovery boundary the release staging pipeline reads its
# file list through. It belongs to the release surface by its callers, every one
# of which is maintainer-only and release-excluded -- release.yml (category 9),
# the .gaia/tests/distribution/ harness scripts (category 3), and the staging
# build documented in .gaia/cli/health/runbook.md (category 10). An adopter
# clone never stages a GAIA release, so nothing there would invoke it. Note
# what is NOT claimed: the script is generic enough to be useful anywhere a
# tracked-file list is built, so the withhold rests on the absent caller alone,
# and shipping it later would be a re-decision rather than a correction.
.gaia/scripts/list-tracked-paths.sh
# The shipped-tree leak assertion: it proves no release-excluded path survived
# into the tree that becomes the tarball. Withheld on the absent caller, the same
# ground the tracked-path boundary above rests on: its one caller is release.yml
# (category 9), and an adopter clone never stages a GAIA release, so nothing
# there would invoke it. Its blocking runner, the sibling bats suite
# .gaia/scripts/tests/assert-no-release-leak.bats, is release-excluded by the
# separate .gaia/scripts/tests entry in category 3. What is NOT claimed: the
# script takes a staging directory and a pattern file as arguments and is
# generic over both, so shipping it later would be a re-decision rather than a
# correction. There is a second, narrower reason to withhold it that the
# tracked-path boundary does not carry: its subject is the maintainer-only
# distribution boundary itself, which is a GAIA-internal concern an adopter has
# no counterpart to.
.gaia/scripts/assert-no-release-leak.sh
# Deterministic lint over GAIA's own hook layer; every runner (shell-lint.sh and its sibling bats suite) is release-excluded, so an adopter clone would carry a check nothing invokes. The repaired hooks themselves ship.
.gaia/scripts/lint-hook-cwd-relative-loads.sh
# Deterministic lint over GAIA's own hook layer, withheld on the same terms as its two siblings above: every runner is release-excluded, so an adopter clone would carry a check nothing invokes. The shared arm it enforces (.claude/hooks/lib/jq-availability.sh) and the hooks reaching it both ship.
.gaia/scripts/lint-hook-jq-availability.sh
# The PreToolUse registration read and blocking oracle the two hook-layer gates share. Both consumers are withheld above, so an adopter clone would carry a library nothing sources.
.gaia/scripts/hook-registration-lib.sh
# The scripts inventory guard fails when a file at the root of .gaia/scripts/ is absent from wiki/concepts/GAIA Scripts.md, where a root file is anything git tracks there or any .sh sitting there on disk, the two sources unioned. Withheld on the same reasoning as the lints above: every one of its runners is release-excluded, so shipping this file would ship a check nothing on an adopter clone invokes. Those runners are shell-lint.sh, excluded by the .gaia/tests entry below, and the sibling bats suite .gaia/scripts/tests/lint-scripts-wiki-inventory.bats via the separate .gaia/scripts/tests entry beside it. An adopter clone does carry both subjects -- it has a .gaia/scripts/ and it receives the page -- so the withhold rests on the absent runner rather than on an empty surface. It carries the same second ground its two siblings above do, and here that ground is the strongest of the three: the page indexes GAIA's own scripts, an adopter's copy of it is the scrubbed subset GAIA ships them, and an adopter who drops a script of their own into .gaia/scripts/ owes no row in a page they did not write. The on-disk half of the subject set makes that concrete rather than hypothetical, because it reds on an untracked file the moment it appears.
.gaia/scripts/lint-scripts-wiki-inventory.sh
# Deterministic lint over GAIA's own hook layer, withheld on the same terms as its siblings: every runner (shell-lint.sh and its sibling bats suite) is release-excluded, so an adopter clone would carry a check nothing invokes. The repaired hooks themselves ship.
.gaia/scripts/lint-hook-monitor-arming.sh

# --- 2. Maintainer-only wiki content ---
# Internal vault administration files and team-specific knowledge that
# describes the GAIA project itself rather than reusable patterns.
wiki/entities
wiki/meta
wiki/.obsidian/workspace.json
wiki/concepts/Release Workflow.md
wiki/concepts/Release-Notes.md
wiki/decisions/Bundle-time Scrub.md
wiki/decisions/CLI-Binary-Split.md
wiki/decisions/Folding Shell Scripts into the CLI Binary.md
wiki/decisions/Forensics Triage Workflow.md
# Describes .github/workflows/audit-ci-tests.yml, which is itself
# release-excluded, so on an adopter clone every claim here is about a
# workflow they do not have.
wiki/decisions/Sharded CI Test Matrix.md
# Documents the fixture-discrimination convention for the maintainer-only shell guards and their shared library, none of which an adopter clone has.
wiki/decisions/Shell Guard Fixture Discrimination.md
# Documents the local runtime of shell-lint.sh (release-excluded via the wholesale .gaia/tests entry below), the mawk/GNU-parallel maintainer tool prerequisites it uses, and the awk-interpreter-pin guard and resolver (.gaia/scripts/lint-awk-interpreter-pin.sh, .gaia/scripts/awk-interp-lib.sh), which govern only the guard-awk-lib.sh closure and are withheld individually elsewhere in this file. Every subject this page describes is maintainer-only; an adopter clone has none of them to run.
wiki/decisions/Local Test Runtime.md

# --- 3. Test harnesses and audit reports ---
# Smoke / observability harnesses run by maintainers in CI; never invoked
# from an adopter's project. .claude/rules/maintainers/ holds maintainer-only
# rules (smoke-harness conventions plus other maintainer-only guidance) that
# must never ship to an adopter.
# .gaia/scripts/tests and .github/audit/tests are bats suites for shipped
# GAIA-owned scripts (.gaia/scripts/ and .github/audit/ helpers). Their only
# runner, audit-ci-tests.yml, is release-excluded (category 9), so they never
# execute on an adopter clone; adopters receive the verified scripts as owned
# code they never edit, so the suites guard only maintainer changes.
.gaia/tests
.gaia/scripts/tests
.claude/rules/maintainers
.github/audit/tests
.specify/extensions/gaia/test
# react-perf-smoke.spec.ts is the bippy version-bump canary: it asserts GAIA's
# capture harness still resolves a known GAIA component (ThemeSwitch) by name +
# memo + timing, and that the StrictMode bypass fires. It guards GAIA's own
# instrumentation against a bippy/React upgrade, not adopter app behavior, and
# is coupled to a GAIA component an adopter may rename or remove. The capture
# helper it exercises (.playwright/react-perf/) DOES ship; this canary does not.
.playwright/e2e/react-perf-smoke.spec.ts
# Asserts that GAIA's own bats suites agree about how they extract a step out of .github/workflows/code-review-audit.yml. Both ends are already release-excluded: the audit workflow (category 9) and the .gaia/scripts/tests and .github/audit/tests suites it enumerates (this category). Its roster is two hardcoded tables, not a derived set, so on an adopter clone carrying none of those suites every entry is stale and the check fails outright rather than sitting inert. Withholding it also keeps its roster from naming .github/audit/tests/ and .gaia/scripts/tests/ paths inside an adopter bundle.
.gaia/scripts/check-step-body-extractor-roster.sh
# This check asserts an invariant true of GAIA's own source: that every Code Audit Team member definition carries the scope-digest obligation literal and captures inside its own scope-resolution region. Three of the five definitions it scans are maintainer-only, and its only runner, the .gaia/scripts/tests/ bats suite, is already release-excluded, so shipping it would ship a check nothing on an adopter clone invokes.
.gaia/scripts/check-scope-digest-adoption.sh
# The audit-residual dismissal store records this repository's own triage
# decisions: assertions about this repository's code that are false about
# an adopter's. The machinery ships and the data does not. Without this
# line the store would ship as an `owned` file, costing an adopter one
# overwrite at the newly-owned transition and perpetual `.gaia-merge`
# patch noise thereafter. The manifest is derived by skipping what this
# file excludes, so the store's absence from `.gaia/manifest.json` is a
# consequence of this line and never the mechanism.
.gaia/audit-residual-dismissals.jsonl

# --- 4. CLI maintainer source + maintainer binary ---
# Adopters receive only the bundled adopter binary at .gaia/cli/gaia (and
# the templates at .gaia/cli/templates/). Source, tests, fixtures, and
# build configs stay in the template repo so adopters can't accidentally
# rebuild the binary out from under themselves.
#
# .gaia/cli/gaia-maintainer is the second bundle produced by `pnpm bundle`
# (entry: src/index.maintainer.ts). It carries the maintainer-only
# `release` namespace and exists only on the GAIA team's clone; adopters
# never see it. The adopter `gaia` binary's source (src/index.ts) does not
# import any release code, so esbuild keeps the release surface out of the
# adopter bundle by construction.
.gaia/cli/src
.gaia/cli/test-fixtures
.gaia/cli/package.json
.gaia/cli/pnpm-lock.yaml
.gaia/cli/pnpm-workspace.yaml
.gaia/cli/tsconfig.json
.gaia/cli/vitest.config.ts
.gaia/cli/eslint.config.mjs
.gaia/cli/prettier.config.mjs
.gaia/cli/.gitignore
.gaia/cli/node_modules
.gaia/cli/dist
.gaia/cli/gaia-maintainer
# Lives under `.gaia/scripts/` but belongs to this category by its inputs: it
# asserts the committed bundles and templates are what rebuilding from
# `.gaia/cli/src` produces, and both that source and `gaia-maintainer` are
# excluded above. On an adopter clone there is nothing to rebuild from, so the
# check could only fail or mislead. Its two callers (cli-tests.yml,
# release.yml) are maintainer-only and release-excluded in category 9.
.gaia/scripts/verify-cli-bundle-fresh.sh

# --- 5. Release-time maintainer tooling ---
# This file itself: adopters don't run GAIA releases. The release-scrub
# config drives `gaia-maintainer release scrub` (bundle-time marker-strip + leak-check)
# and is consumed only by the maintainer's release.yml. Note:
# `.gaia/scripts/` DOES ship: `check-updates.sh` is the background refresher
# invoked by the adopter-facing statusline (writes the cache the statusline
# reads for `Run /update-deps` and `Run /update-gaia` indicators).
.gaia/release-exclude
.gaia/release-scrub.yml

# --- 6. Maintainer dev-tool configs ---
# Per-machine tool configs initialized by `/setup-gaia` on the adopter's
# side; the template's copies aren't portable. Keep these out of the
# manifest so /update-gaia never tries to merge them.
.serena

# --- 7. Scratch + transient ---
# Local working directories: scratchpad ingestion, per-machine state
# (including the shared update-tarball cache at `.gaia/local/cache/shared/`),
# update-merge stage area, init backup. Per-machine SPEC state (the ledger at
# `.gaia/local/specs/ledger.json` plus the SPEC folders) lives under
# `.gaia/local/`, already excluded by the line below; a fresh adopter starts
# SPEC numbering from an empty local ledger and an unseeded remote `spec/*`
# tag namespace.
.raw
.gaia/local
.gaia-backup
.gaia-merge

# --- 8. Per-machine plan / handoff / agent state ---
# Generated at runtime under the user's clone; not template content.
.claude/worktrees
.claude/agent-memory

# --- 9. Maintainer-only CI workflows ---
# `release.yml` cuts releases of the GAIA template itself (triggered by
# `v*.*.*` tags); adopters never release GAIA. `cli-tests.yml` runs the
# .gaia/cli/ vitest suite; adopters receive only the bundled binary, so
# the test workflow has nothing to test on their side.
# `audit-ci-tests.yml` runs the bats suites for the `.github/audit/`
# helpers (resolve-audit-base.sh, resolve-check-base.sh) that
# code-review-audit.yml consumes; adopters receive those scripts as
# GAIA-controlled code they never edit, so the suite guards only
# maintainer changes. `forensics-triage.yml` runs autonomous Claude Code
# triage against `gaia-forensics`-labeled issues on the upstream
# `gaia-react/gaia` repo; the workflow's helpers under `.github/forensics/`
# include scope checks, classifier prompts, fixtures, and bats tests that
# only matter on the maintainer's clone. `code-review-audit.yml` installs
# on demand via `/setup-gaia` (like the cron workflows) rather than
# shipping by default, so adopters who have not enabled GAIA CI never carry
# a credential-less audit that blocks every merge. `shell-lint.yml` runs
# shellcheck over GAIA's framework bash via the `.gaia/tests/shell-lint.sh`
# harness (already excluded by the `.gaia/tests` entry in category 3); adopters
# run that bash but never author it, so the linter guarding it has no adopter
# surface. All would be silent passengers at best and confusing CI failures
# at worst on adopter clones.
.github/workflows/release.yml
.github/workflows/cli-tests.yml
.github/workflows/audit-ci-tests.yml
.github/workflows/forensics-triage.yml
.github/workflows/code-review-audit.yml
.github/workflows/shell-lint.yml
# `cli-advisory-scan.yml` runs the advisory arm of
# .gaia/scripts/check-cli-workspace-floors.sh on a schedule, against .gaia/cli's
# dependency closure. Excluded on the same reasoning as the check itself
# (category 1 above): .gaia/cli is a second pnpm workspace root that exists only
# on this clone, so on an adopter's the job would red daily over a directory they
# do not have.
.github/workflows/cli-advisory-scan.yml
.github/forensics

# --- 10. Maintainer-only health-audit infrastructure ---
# `.gaia/cli/health/` holds the audit taxonomy, run state, and (post-#97)
# the orchestrator's per-cycle outputs. Only maintainers run health audits
# against the GAIA template; adopters audit their own app via the standard
# `code-review-audit` agent. The bundled `gaia` binary does not import
# from `src/health/`, so esbuild tree-shakes any future health code out
# of the adopter binary by construction.
.gaia/cli/health
.gaia/cli/src/health

# --- 11. Maintainer-only project governance ---
# Adopters use GAIA as a template via `npx create-gaia` and run independent
# projects with their own license, contributors, changelog, README, and
# code-of-conduct decisions. Shipping GAIA's copies makes their project
# inherit GAIA-the-template's governance, wrong by default. The README
# is regenerated by `/gaia-init` from `.gaia/templates/README.md` (which
# DOES ship); the others are adopter responsibility. CODEOWNERS encodes
# who owns code review on the GAIA repo; an adopter's owners are their own,
# so `/gaia-init` writes a fresh `.github/CODEOWNERS` (`* @<their-username>`)
# during setup rather than inheriting GAIA's.
.github/CODEOWNERS
CHANGELOG.md
CODE_OF_CONDUCT.md
CONTRIBUTING.md
LICENSE
README.md
SUPPORTERS.md

# --- 12. Maintainer-only README assets ---
# The root README.md (excluded above) is the sole consumer of these brand
# images, and it is maintainer-facing: adopters get a fresh README regenerated
# from `.gaia/templates/README.md`, so the GAIA logo it embeds has no business
# on an adopter clone. The asset lives outside `app/` so it is never pulled
# into the app build either.
docs/assets
