// GAIA UAT — auto-generated by /speckit-gaia-uat-write
// SPEC: ${SPEC_ID} | UAT: ${UAT_ID} | generated: ${GENERATED_AT}
//
// Given: ${UAT_GIVEN}
// When:  ${UAT_WHEN}
// Then:  ${UAT_THEN}
//
// GAIA UAT — divergence policy
// Cosmetic divergence (selector text, button label, copy): editable.
// Logical divergence (flow, success criteria, error handling): forbidden.
// Full rule: ${DIVERGENCE_RULE_PATH} (skeleton; canonical owner is the verifier SPEC).

import {test} from '@playwright/test';

test.fixme('${UAT_ID} — ${SPEC_ID}', async () => {
  // Abstraction blocker: ${ABSTRACTION_BLOCKER}
  // Original UAT:
  //   Given: ${UAT_GIVEN}
  //   When:  ${UAT_WHEN}
  //   Then:  ${UAT_THEN}
  //
  // The implementer reads the blocker, raises it to the PO, and the SPEC is
  // reopened to refine the UAT. Hook never silently drops a UAT.
});
